[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "inventor-workshop"
version = "0.5.0"
description = "A shared autonomous workshop for toys and board games"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
workshop = "inventor_workshop.cli:main"
inventor-workshop = "inventor_workshop.cli:main"
inventor-foundation = "inventor_workshop.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
