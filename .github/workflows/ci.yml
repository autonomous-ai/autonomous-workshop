# Inventor CI — deterministic, credential-free gates for the shared Foundation and
# every native inventor harness. Imported upstream snapshots are validated by
# their pinned provenance records rather than by installing their undeclared
# or environment-specific dependency trees.
name: inventors-ci

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  repository-policy:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: reject tracked secrets and backups
        run: python foundation/tools/scan_secrets.py
      - name: validate inventor registry
        env:
          PYTHONPATH: foundation/src
        run: python -m inventor_core registry --root inventors --check-entrypoints
      - name: verify pinned inventor snapshot bytes and modes
        run: python foundation/tools/verify_snapshot_locks.py

  foundation-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Foundation contract tests
        env:
          PYTHONPATH: foundation/src
          PYTHONWARNINGS: error
        run: python -m unittest discover -s foundation/tests -p 'test_*.py' -v

  bob-test:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.12"]
    env:
      BOB_MOCK_AGENTS: "1"
    defaults:
      run:
        working-directory: inventors/bob
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: install Inventor Foundation
        run: python -m pip install ../../foundation
      - name: unit tests (stdlib unittest only, no pytest)
        run: python -m unittest discover -s tests -v

  eve-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.9", "3.12"]
    defaults:
      run:
        working-directory: inventors/eve
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
          cache-dependency-path: inventors/eve/requirements-test.txt
      - name: install shared Foundation and pinned Eve test dependencies
        run: |
          python -m pip install ../../foundation
          python -m pip install --disable-pip-version-check -r requirements-test.txt
          python -m pip check
      - name: deterministic offline tests
        env:
          PYTHONPATH: .
          PYTHONWARNINGS: error
        run: python -m pytest -q -ra

  alice-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.13"]
    defaults:
      run:
        working-directory: inventors/alice
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: install Alice and shared inventor Foundation
        run: python -m pip install ../../foundation .
      - name: unit tests (warnings are errors)
        env:
          PYTHONWARNINGS: error
          PYTHONPATH: src
        run: python -m unittest discover -s tests -p 'test_*.py' -v
      - name: installed CLI smoke test
        env:
          PYTHONWARNINGS: error
        run: |
          runtime="$RUNNER_TEMP/alice-smoke-${{ matrix.python-version }}"
          alice --root "$runtime" init
          alice --root "$runtime" learning-init
          alice --root "$runtime" eval
          alice --root "$runtime" doctor

  alice-macos-service:
    runs-on: macos-latest
    timeout-minutes: 20
    defaults:
      run:
        working-directory: inventors/alice
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"
      - name: install Alice and shared inventor Foundation
        run: python -m pip install ../../foundation .
      - name: service snapshot tests on the declared deployment OS
        env:
          PYTHONWARNINGS: error
          PYTHONPATH: src
        run: python -m unittest discover -s tests -p 'test_service.py' -v
