# Cratercade CLI run notes

Started 2026-09-10 at 13:29:33 UTC from repository commit `61da5eaf`.
This is the first build from the [KiwiCo-inspired shortlist](kiwico-inspired-toys.md).

## Current checkpoint — CLI resumed 2026-09-11 13:16 UTC

Normal CLI handle 92434 resumed the exact native session at 13:16:35 with
Astra medium and the 500M total cap, without refreshing frozen tools. Fresh
reasoning, commentary and tool activity are confirmed; usage at 13:16:49 is
336,582,614 tokens. Host fix `41fd7b2f` preserves two exactly recognized native
transport reconnect notices within the same process. The full runtime suite
passes 111 tests with one skipped; an actual Codex 0.153.4 loopback fixture
passes through the host stream with two requests, one process/thread, exit
zero and exact post-cleanup usage reconciliation. Independent review is clear.
No stage gate or product check has been waived. Publication is not-created.

At 13:39 the CLI remains live, with 350,578,175/500,000,000 tokens recorded.
The nut representation qualification completed: 199 actual installed leaves
plus three jackpot trim angles pass, with selected STEP round trips also
passing. The native Manager accepted the handoff and applied the NURBS
conversion in `parts/purchased_hardware.py`; its new SHA is
`cec7478d81a8174a4bee38dc90157acb46668c8d299307c54b5549e7aa245dbe`.
The original supplier STEP remains unchanged. Local inventory and balance
calculations use adaptive properties with their existing tolerances.
`design/jackpot-balance-r8.json` passes and explicitly records physical_test
false; SHA is `6627358fff1f4b3fe2c621de2c6223e73d6d206f3b9fdfe8e57a95eaa72eda76`.
The repaired combined export is complete. Full native validation r8 has
started but has no verdict yet. The inventory step failed with
`ValueError: Null TopoDS_Shape object`; no new manifest was written, so that
step still needs repair. Full motion has not produced a result. Optional
source guidance `d77a0301` remains outside the frozen run.

By 13:59, native validation r8 passes all 542 occurrences with zero failures,
and native interference passes with zero clashes. The batch's before/after
source manifests match across all 148 files. Round eleven passes numeric and
visual feedback (`6af8b62542a8c8b48ad7c306b37b6bbb57ef969bd12e970c7528a3b8eb115c6d`),
reusing all 78 part roles. All 78 regenerated printable STEP files are
byte-identical to their previously qualified revision, and all 156 print
report hashes remain qualified. The normalized inventory candidate then
passed all 398 hardware placements. Thirteen nut pose records changed to
equivalent representations, passing the unchanged identity tolerance.
The Manager applied the inventory helper repair after the earlier batch;
`measure/build_mounts.py` is consequently newer than that batch's source
binding. Current mounting and load/reset checks and a further Make round are
refreshing the downstream evidence. Full motion and final presentation have
not completed. Usage at 13:59 is 361,767,988/500,000,000, with CLI 92434 live.

At 14:12, CLI handle 92434 is confirmed live, with 368,366,004/500,000,000
tokens observed. `design/native-mount-static-r9-summary.json` passes all
398 mounting placements and both 542-occurrence mission layouts with zero
static clashes and stable before/after bindings. The native Manager reports
the canonical inventory matches the qualified candidate. The current r12
handling summary has five completed passing checks: all three load diameters
and reset diameters 15.5 and 16 mm. Root checked each report's exact recorded
hash and its passing/source-stable fields. The 16.5 mm reset sweep and final
isometric view remain pending; the round twelve summary is not yet present.
All 542 production targets are prepared, but export is waiting on the round's
source freeze. Full motion still has no verdict, and final review, integrated
verification, Made output and authenticated publication remain incomplete.

By 14:15, all six r12 loading/reset reports pass with matching recorded hashes
and source-stable fields, including the final 16.5 mm reset sweep (1,571
samples). Round twelve has `ok: true`, `checks_ok: true`, and passing visual
feedback bound to packet
`e865084bb15600e6afffeee8fe9efc44b525252c4a7adbf1ad49cfb17dab2a56`.
Root viewed its new isometric image. The Manager reports that the regenerated
motion manifest is semantically identical to the canonical declaration and
has started the full native check across 1,303 conditions. Production exports
and motion presentation are next. CLI 92434 remains live; observed usage is
369,919,745/500,000,000. No full-motion verdict or publication exists yet.

At 14:24, root verified the R12 review's packet hash, all three image hashes,
and all 230 bound source/export hashes against current bytes; no mismatches.
CLI 92434 remains live at 373,160,535/500,000,000 observed tokens. The current
motion result is `design/check-motion-r6.json` (still empty, no verdict),
not the interrupted r5 report. Current exports use `production-build-r9.log`;
motion presentation uses `motion-presentation-r6.log`. Both jackpot endpoint
exports are complete per native commentary and the comparison is rendering.

By 14:32 the canonical hero and paired jackpot stills are present. Root viewed
`product/cad/snap/signature.png`: the bucket and flag change angle, while a
foreground post obscures the linkage center and the roof obscures the raised
flag tip. `design/independent-review-r1/blind-stills.json` records the critic's
blind reading, with both input hashes matching current images. It recognizes
ball handling and suggested linked rotation, but also notes unclear controls,
ball path and occluded coupling. Its verdict is null pending animation and
the required comparison with the Wish; this is not a passing final review.
CLI 92434 is live with 374,328,260/500,000,000 tokens observed. Full motion and
animation remain pending; authenticated publication has not started.

At 14:51, production r9 reports all 542 files generated with source_stable
true. Root checked the actual delivered package: `assembled.step.json` has
542 occurrences, its STEP hash matches `assembled.step`, and all 542 files
in `product/parts/` match their corresponding freshly generated STEP bytes.
This closes the earlier stale 512-occurrence delivery inventory issue, but
does not establish final integrated verification or publication. Motion r6
still has no verdict and the animation remains unfinished.

At 15:12, CLI handle 92434 is confirmed live with 378,459,756/500,000,000
tokens observed. Native delivery audit also passes and all nine group receipts
are refreshed. The full r6 motion check, started 14:13:38, and r6 animation
generation, started 14:14:24, remain active with no completed output or
reported error. Final motion/signature reviews, integrated verification and
Made output are absent; publication remains not-created. This is a verified
wait on the existing CLI, not a terminal failure or permission blocker.

The following describes the preceding interruption and recovery evidence.

Handle 88872 stopped at 10:32:39 with an
unclassified native error; host cleanup succeeded. A read-only audit verified
all 243 frozen inputs, 2,845,424 bytes, modes and inventories against stable
checkpoint revision 11. Ordinary handle 32800 resumed the exact native session
at 11:03:57 with Astra medium and the 500M cap, without refreshing tools. It
performed fresh work, then stopped at 11:06:15 with the same diagnostic shape:
top-level `error`, 39-byte message, no code, unclassified. Cleanup succeeded
again; no Workshop timeout was set. Recorded usage is 336,453,385/500,000,000.
The raw error was not retained, so its cause is not established. The prior
account-limit interruption remains distinct from this unexplained failure.

Two credential-free native transport fixtures proved that Codex 0.153.4 can
emit a top-level `error` as a nonterminal reconnect notice, retry in the same
process and thread, and complete successfully. The observed messages are
100 and 89 bytes, so they do not identify the historical 39-byte failures.
A narrow host fix has now passed testing and review as recorded above.
Both 88872 and 32800 are terminal; do not poll or restart them. Make remains
unfinished. Optional source updates `88d9f9a8`, `873f33ca` and
`046e11b6` remain outside the frozen project. Publication is not-created.
The earlier 3–5-hour estimate is no longer dependable. Several more hours
after stable native execution is a planning allowance, not a promised finish.

The latest cup repair is implemented and passes its bounded source checks.
Report `design/collection-nut-relief-r4/report.json` binds current cup SHA
`63703e23f6b41d6d0545b5a78d89eaf52f2e8679c91b1e886b06c9003f0e7a8e`.
An exterior notch removes 14.9695 mm³, adds no material, clears the catalog
nut by 0.25 mm and retains 1.70 mm of inner wall. It preserves the floor,
pickup lips, mount regions and return opening. One valid solid remains.
Round ten passes its numeric and visual checks: one changed cup checked and
77 part types reused. The Manager inspected all three views; packet hash is
`343239a9627f89b451fd03672070b1105e4f501591f9b4396143509a52173f3b`.
Canonical print reports were subsequently refreshed, and the combined STEP
was regenerated after the round. The native mounting report passes all 398 mounts, and both
542-occurrence mission assemblies have zero static clashes. Their combined
summary passes with stable source bindings. All six loading/reset reports
also pass for 15.5, 16 and 16.5 mm marbles; root independently verified all
148 source bindings in each report still match current CAD.

Round nine passed all 156 print checks and visual review on the preceding
source. Subsequent strict bed-fit checks passed all 78 types, but the
398-mount gate and both complete mission assemblies found the same single
cup/nut collision, motivating the repair above. Native Make interrupted
full motion, movie generation and delivery export with no completion verdict;
partial work and failed evidence are preserved. The earlier combined package
contains 542 occurrences. All earlier print, fit, return, pickup/loading,
reset and balance passes retain their original source bindings and require
appropriate reconciliation after this latest edit.

The separate native topology validator fails on 185 nut occurrences, all
with `selfIntersecting`, one positive-volume solid and no other reported
reason. Native interference reports zero clashes, but that does not override
the validation failure. The source stayed unchanged during those checks.
Root's independent source review has not established a tool defect. A
synthetic nut passes seven rigid poses directly and through scene traversal,
including repeated/reversed-order checks with stable BREP hashes; invalid
controls are correctly rejected. Source-target inspection evaluates fresh
in-memory geometry, not the disk viewer package. Native Make reports that
the actual supplier nut passes before placement, fails under the board tilt
and passes after inverse transformation; it is testing a geometry-preserving
representation repair with the unchanged validator. That qualification and
accepted repair are recorded above; the original failed report remains
historical evidence until fresh full validation passes.

Camera diagnostics selected a 30°/10° direction that reveals the flag, bar,
bucket and marble in both endpoint states. Final framing and current-source
rendering remain pending. Full native motion verification restarted at
10:18:34 against the repaired source and has no verdict; the later native
session failure ended the run, and no surviving motion job is claimed. The regenerated
motion declarations are byte-identical to the preceding declaration file;
the canonical compact bytes are reused with exact parsed equivalence.
Exact endpoint exports were started, but native Make stopped animation at
10:29 while diagnosing the nut representation, anticipating a source change.
Full motion had continued to collect other failures before the session stopped.
Full motion,
resolution of the topology finding, refreshed affected mechanical evidence,
delivery files, independent review, integrated verification and authenticated
publication remain unfinished. These are digital geometric checks; physical
fit, rolling, force, wear and play quality remain untested.

## Request and execution

Create a primarily 3D-printed lunar pinball arcade with two independent
flippers, a rubber-band plunger, rearrangeable crater obstacles, two mission
layouts, and a marble-weight-driven bucket-to-flag jackpot. Ordinary marbles,
rubber bands, felt, and optional card are allowed. No electronics.

Run ID: `wish-20260910-132933-a88b698e`.
Initial runtime: Codex, `gpt-6-astra`, `ultra`.
Workflow: Spark. Initial total product token cap: `100000000`.
Current operator selection: medium effort and a 500M total cap, applied by the
normal 02:55 resume after adopting the other session's completed fix. After
the 01:59 normal resume,
root metadata and both newly started native child contexts confirm medium.
Older child threads retain their historic ultra metadata; no new turns on
those threads were observed in that continuation's verification window.
Root native session: `01a08b82-68d1-72c0-8876-a9a9074885d8`.
Inventor: Bob, selected by Workshop's native Manager.
Public publishing account: Dee, explicitly requested by the user and verified
through Factory authentication on 2026-09-10. The authorizing account and the
selected design Inventor are separate identities.

The run was launched using the real installed `.venv/bin/python -m cli wish`
entrypoint with `--workflow spark --agent codex --model astra --effort ultra
--max-tokens 100000000 --strict`. `WORKSHOP_CODEX_BIN` selected the installed
supported Codex 0.153.4 native executable. The exact long command is retained
locally in the ignored `.tmp/cratercade/launch-command.sh`; the host has the
immutable complete Wish. This document contains observations, not raw session
transcripts or effect credentials.

## Observed progress

- 13:29:33 UTC: normal CLI initialized the private product run and displayed
  the requested model, effort, and 100M token allowance.
- 13:31:49 UTC: native inventor selection finished. Workshop accepted Bob and
  resumed the same root session for Make.
- 13:32:52 UTC: CLI status showed active Make, the same model/effort, and
  457,453 observed tokens out of 100,000,000. This was an intermediate reading
  excluding in-flight work, not a final cost.
- A user-requested `git pull` during the run reported already up to date;
  no run restart or frozen-workspace mutation occurred.
- 13:44:08 UTC: the CLI exited 2 during Make with `native usage file lacks
  session identity`. The native session remained checkpointed. Research and
  the requirements ledger were present; the complete CAD product was not.
- Before recovery, a read-only scan validated all 18 recent native rollout
  metadata records. The unchanged production usage reader reconciled five
  root/descendant threads to 5,660,803 tokens. The failing read was transient;
  the original error did not identify which candidate file caused it.
- 13:46:56 UTC: normal `workshop resume wish-20260910-132933-a88b698e --strict`
  resumed the exact native session. Its frozen settings and 100M-token cap
  remain in force; no run files or token counters were manually repaired.
- 13:54:58 UTC: Make's native hardware researcher completed its reference
  handoff. It detected that a catalog model labeled as an M4 hex nut had eight
  external sides and rejected it. A six-sided M4 thin nut was qualified as an
  alternative, with final adoption and interfaces left to the Manager. The
  researcher also distinguished a simplified screw model from larger
  commercial head/thread envelopes. This is useful digital inspection, not
  proof of purchased-part fit or a finished toy.
- 14:08 UTC inspection: Bob's paired construction handoff changed from pending
  to complete. It specifies a six-tile 320 × 480 mm deck, independent 46-degree
  thumb flippers, an 18 mm band launcher, two coordinate-defined missions,
  and one rigid bucket/flag rocker with a declared balance/torque check.
  Covered ball return and cut-to-size clear craft-sheet containment are also
  specified. These are design targets; actual CAD, verification, and physical
  behavior remain unproven at this milestone.
- 14:24 UTC: normal `workshop login bob` completed the browser connection to
  Dee. A fresh host authentication of the saved, correctly bound credential
  verified public username `dee`. This repairs the prior invalid per-Inventor
  configuration and lets the existing run publish as requested without a
  native session restart. No credential values entered the toy workspace,
  source, notes, or native agent.
- First CAD inspection round: the STEP build succeeded, but the visual review
  explicitly failed the partial blockout. Clockwise prism profiles extruded
  the ramp and bucket floors in the wrong direction; native review measured
  the ramp at X-48..16 instead of X-16..16 and specified a +X extrusion repair.
  Flippers, launcher, and ball-contact states were not yet in those images.
  Print checks also correctly refused the non-printable assembly entry; they
  must run on individual printable parts. No early proof or print pass is
  claimed from that round.
- Preliminary jackpot balance audit: 57 poses from 0 to -28 degrees passed
  the native quasi-static checks. The model reports empty restoring torque
  0.095–0.163 mN·m, at least 0.810 mN·m loaded tipping torque for a 4 g marble,
  supported free-ball contact, and at least 2.035 mm deck clearance. It uses
  assumed solid-PLA and steel densities and excludes friction. Final geometry,
  print settings, and mass assumptions must still match the evidence; this is
  not a physical tipping or return test.
- Second mechanism inspection round: 12 of 18 individual print entries
  passed; two flipper guards, the jackpot frame, landing ramp, launcher
  housing, and launcher rod required repairs. The three requested renders
  completed successfully, but the visual packet was correctly rejected:
  `cratercade.step` changed during rendering. Native tool-call metadata
  confirms a separate `gen cratercade.step.py --write --force` invocation
  at 14:39:05 UTC, inside that render window. Generated images and renderer
  caches are excluded from source hashing; no guard defect or tool refresh
  was found. Two focused existing tests passed.
- 14:47 UTC inspection: native repairs have produced passing local thickness
  and overhang reports for the flipper guards and launcher. The flipper
  BRep audit reports no failures across 47 sampled control positions, and
  the launcher audit reports retained travel and no tested stroke/service
  collisions. These are component checks, not a passing integrated round
  or a finished arcade; the full assembly and final evidence are pending.
- 14:53 UTC inspection: the refreshed jackpot audit now includes 25 fixed
  obstacles and checks both hard stops. The rocker clears the sampled working
  range and intersects the frame at 0.5 degrees beyond either endpoint;
  installed axle caps also obstruct 0.5 mm axial escape in both directions.
  All seven declared checks pass. Fastener retention, final assembly coverage,
  exact evidence binding, and physical mass/friction still need their own
  verification; the report explicitly preserves those limits.
- 14:55:50 UTC: the resumed CLI exited 2 after an explicit native failed-turn
  event. Its bounded diagnostic classified the error as unknown; raw error
  text is deliberately not retained. Recorded spend was 30,609,976 of
  100,000,000 tokens. An already launched deterministic third CAD round
  finished at 14:57:43 with a valid visual packet pending Manager review and
  remaining print failures. A subsequent narrow process check found no
  remaining run-specific CAD writer.
- 15:14:28 UTC: the ordinary `workshop resume` command started the exact native
  session again. The frozen Wish, Astra ultra settings, token accounting,
  artifacts, and Dee publishing connection remain in place. No tools were
  refreshed and no gate evidence was manually changed.
- 15:21:58 UTC: that attempt also ended with an unclassified native terminal
  error. Recorded spend reached 31,238,920 tokens. Both failure records report
  a 39-byte message with no safe provider code; that does not establish that
  the messages or causes were identical. Normal Codex logs contained no
  attributable root-session diagnostic. Investigation is improving exact
  known-error classification before another same-session recovery attempt.
- Third-round result: 17 of 18 individual part entries pass thickness and
  overhang checks. Only the landing ramp's overhang check still fails. The
  render packet is valid and awaits native Manager feedback; motion evidence,
  the full table, and final verification remain pending.
- 15:40:43 UTC: resumed the same session after validated diagnostic
  improvements and a fresh process check showing no remaining native/CAD
  process for this run. Geometry and frozen product tools were preserved.
- 15:45 UTC inspection: the native Manager recorded third-round visual
  feedback and preserved its failing ramp result. New bounded native workers
  are implementing the six-tile deck/feet and the perimeter, crater, and
  two-mission playfield; another prepares controls motion evidence. Handoffs
  explicitly say work is in progress. The ramp repair raises its exit and
  steepens the unsupported underside; fresh print and marble-path evidence
  remain required. This is progress toward the complete arcade, not a Make
  gate pass.
- 15:57 UTC inspection: the ramp's fresh local print reports pass both gates.
  Six deck tiles, seam straps, and feet have saved generated print bounds
  within 200 mm. Native integration moved the return to Z-30 and the left
  straps to X99, clearing the main hood by 2 mm; it then narrowed strap webs
  and end necks, also resolving a potential return mounting-boss overlap in
  the current source. These are component/interface observations. The complete
  mounting inventory, deck cuts, assembly collisions, and final evidence still
  require verification.
- 16:04 UTC inspection: all 13 playfield part types have successful local
  thickness/overhang command exits. The two-layout geometry report finds no
  declared static or mechanism clashes. A separate 22 mm free-space gauge
  route check still fails on proposed field paths, including the orbit exit
  and launcher divider; it explicitly does not simulate physical ball motion.
  The controls audit also identified a launcher bolt-service conflict with
  its neighboring post. Native repair/integration continues; these failures
  must be recovered in current geometry and final evidence.
- 16:08 UTC inspection: the revised free-space route report passes all four
  A/B orbit and field paths with the same 22 mm gauge. Saved A-isometric and
  B-top renders visibly include the perimeter, orbit, craters, and different
  guide placements around the mechanisms. They still use a neutral proof
  deck; complete structure/return/containment and final motion evidence remain.
- 16:20 UTC inspection: the assembly now includes deck, playfield, return, and
  hardware. The first return-family print checks pass seven of 13 entries;
  four hoods, the collection cup, and hopper funnel need repairs. The deck
  handoff also identified the ramp-seat ceiling as unprintable in its current
  face-down pose and proposed a coordinated deck/ramp repair. The controls
  handoff has 107 conditions and 43 directional retention rows for root
  integration, not a final native motion pass.
- Canopy source now replaces the draft grooves with a sandwich frame:
  160 × 144 mm cell, 144 × 128 mm aperture, and 156 × 140 mm PET with four
  16 × 16 mm corner notches. Edge overlap is 6 mm. Its 1 mm separation captures
  nominal 0.35 mm PET loosely; it does not compressively clamp it. Final cuts,
  post/support geometry, sheet escape, side-sheet dimensions, and complete-cell
  loading/reset service must match the actual final assembly. Cap fasteners
  and cell-release fasteners must be distinguished in the instructions.
- 16:32 UTC inspection: the initial combined 316-occurrence source audit found
  26 clashes; native structural/interface repairs are in progress. Root's
  return-clearance report passes 414 source-BRep samples with the actual
  16.5 mm marble, return solids, and hardware. It replaces an invalid vertical
  edge-drop path with a geometric rolloff arc into the lower cup; this remains
  clearance evidence rather than a simulated or tested physical trajectory.
  Return local print failures have narrowed to hood-pocket repairs, with
  restored geometry being rechecked. Native band work covers 205 deforming
  states and explicit solid self-interference; it is still running.
- 16:39 UTC inspection: the repaired structural comparison passes with no
  clashes or invalid solids in its declared scope. All 205 nominal deforming
  band states pass; 8,815 band/rigid-obstacle pairs have no sampled overlap,
  and a fresh-process source/parameter probe matches the audit fingerprint.
  The report explicitly excludes future walls/aprons/canopy and marble
  contact, physical force/fatigue, and unsampled states. Full assembly
  integration must cover those geometric neighbors; physical behavior is
  still untested.
- 16:49 UTC containment inspection: the latest local closed/static report has
  345 candidate pairs, no overlap above 0.001 mm³, and 176 valid single-solid
  containment/hardware entries. It excludes apron, deck/feet/straps, jackpot,
  and service sweeps. Each cell now has four corner release bolts and four
  separate midpoint sheet-cap bolts; front-right loading and rear-left reset
  cells can be omitted in the service state. Actual lift/driver access and
  ordinary mounting-nut handling remain unproved; part omission is not an
  extraction-path test. Check that final replay instructions are practical
  and do not claim captive nuts or a two-screw release without real geometry.
  Updated roof cuts also require four 9 × 11 mm midpoint notches, in addition
  to the four 16 × 16 mm corner notches. Side and end sheets are respectively
  131.4 × 72.4 and 148.4 × 72.4 mm in this snapshot.

- 17:08 UTC inspection: the current full jackpot balance audit passes at trim
  Y−55, including its sampled fixed-solid check; calculated empty restore is
  0.24571–0.32493 mN·m and minimum loaded tipping torque is 0.64784 mN·m.
  The separate 83-condition source motion audit does not pass: seven service
  or bolt-seat conditions fail, and source/parameter bindings changed during
  its execution. Final native motion evidence remains required. All twelve
  return/hopper/cup print entries now have passing affected-source local
  reports. Canopy post print repairs pass, and both aprons pass overhang in
  revision four, but apron wall thickness still fails; these are not complete
  print passes. The draft manual now accurately distinguishes four corner
  release bolts from four midpoint cap bolts, includes both PET notch sets,
  and specifies solid PLA for balance-dependent parts. New roof nut traps
  exist, but the service assembly currently omits eight stationary mounting
  nuts that the manual says stay trapped. This is an unfinished service-state
  representation inconsistency, not proof the physical access fails.

- 17:19 UTC inspection: the bounded canopy handoff is complete. Its service
  state now preserves the eight stationary mounting nuts, resolving the
  earlier representation mismatch. Nut cages have explicit insertion and
  directional restraint checks; two complete roof cells clear nine sampled
  lift positions. A small tipped-flag/splice overlap was repaired with a
  local underside relief, and all 29 declared jackpot poses now clear the
  canopy. The final ramp-seat tile overhang check passes. Both aprons and
  their repaired guard bases now pass local thickness; the apron clearance
  report passes 94 flipper poses with its stated thumb-clearance scope.
  The jackpot handoff reconciles all 83 source conditions and records a
  passing native coupled-motion condition across 140 samples. These remain
  bounded checks; final assembly, every print entry, all hardware, final
  review, and integrated verification are still pending.

- 17:29:51 UTC: the fifth Make native attempt ended explicitly with exit 2.
  Its preserved terminal diagnosis is unclassified with a 140-byte message;
  it is not the earlier 39-byte diagnostic. A separate builder helper reported
  an account usage limit, but the native failure record alone does not prove
  that exact cause. Recorded product usage is 89,142,814 tokens. The already
  running band check finished at 17:41:19: all 94 geometric cases completed
  without failures, but overall PASS remained false because source bindings
  changed. No final product gate or publication occurred.
- 23:37:09 UTC: after confirming no remaining process for this run/session,
  resumed through the normal CLI with the same identity, frozen tools and
  original 100M cap. The new native attempt is active and using tools. By
  23:39, complete-product round r0004 is regenerating print entries; native
  usage is 90,619,170 tokens. No scope or budget reset was performed.

- 23:50:01 UTC: the host stopped native attempt six on the original 100M
  allowance, with 100,142,201 recorded tokens. The observed-usage stop may
  include the last in-flight request; it is not a provider hard cap. The user
  subsequently authorized a 500M total allowance (superseding an intermediate
  200M request) and medium effort. Another session owns the 500M CLI ceiling
  update; this builder stopped its duplicate budget task before any edits.
  This builder is implementing an explicit, private host-recorded medium
  override for the same profile-bound Codex session. Neither new setting has
  yet been applied to the run. At 23:51, deterministic CAD subprocesses still
  exist and continue saving the already-started fourth round. Do not resume
  over those writers or claim a completed gate from partial reports.

- 00:12 UTC on 2026-09-11: complete-product round r0004 is terminal and does
  not pass. All 71 print entries generated and all 71 thickness reports pass.
  Five overhang reports fail: deck 0,1; deck 1,1; deck 1,2; rear foot; and
  return hood 3. The render command timed out after 900 seconds without PNGs,
  so there is no valid visual packet from this round. Two source geometry
  checks remain active after the native budget stop. Process ownership was
  verified against the exact Cratercade workspace; an unrelated toy's CAD
  processes were excluded. No failed check was waived.
- 00:27:45 UTC: used the normal `resume --effort medium --strict` command
  without increasing the still-exhausted cap. The host saved the requested
  private effort history, prepared the stage, and stopped before a new native
  turn on the existing token limit. Status now reports `effort: medium` and
  `initial_effort: ultra`, with usage unchanged at 100,142,201. The 500M
  ceiling change is still owned by the other session and has not landed in
  this shared checkout. Subsequent resumes retain medium unless explicitly
  changed again.
- 00:30 UTC: applied the tested source-tool fixes using the normal
  `resume --effort medium --refresh-tools --strict` command. The host recorded
  exactly four changed files: CAD mount indexing, CAD generation import scope,
  Make-round guidance, and the Make-round command. It rebound the instruction
  tree through its owner-only correction ledger; the native thread and runtime
  configuration hash stayed identical. No CAD geometry, usage, or cap changed.
  The existing cap again stopped execution before a new native turn. Remaining
  supplemental checks read CAD and write their own design reports; the global
  round/render process is already terminal. This is the first tool refresh of
  this run; earlier statements about untouched frozen tools describe their
  respective earlier milestones.
- 00:39 UTC: the reset-path audit finished with stable source bindings but
  fails five of its 401 sampled positions. The moving bucket/flag intersects
  `canopy_roof_splice_1` near −23.5 to −23.2 degrees, with a maximum reported
  overlap of about 0.02188 mm³. The earlier 29-pose canopy sample did not
  detect this intermediate contact. This prescribed-path audit does not prove
  physical hand access or dynamic reset timing. The stationary-part audit is
  still running in the exact Cratercade workspace (PID 77388); the reset
  worker has exited. The native model remains stopped; medium and refreshed
  tools are installed, while the shared source maximum is still 200M and the
  user-authorized 500M change from the other session is pending.

- 00:45:51 UTC: resumed the exact native session through the normal CLI with
  `--max-tokens 200000000 --effort medium --strict`. The shared 500M ceiling
  fix is still pending, but its absence does not require stopping useful
  product work: the supported 200M cap is temporary and within the user's
  authorized 500M total allowance. Status confirms 200M, medium, and all
  100,142,201 previously charged tokens retained. Native progress is live
  under CLI handle 51838. A later normal resume can raise the cap to 500M
  after that source change lands; do not run a concurrent host invocation.
  The remaining stationary audit was confirmed live immediately before this
  resume, with 50 supplemental reports written. It reads its loaded CAD and
  writes versioned design reports, rather than exporting shared geometry.
- 00:46:11 UTC: CLI handle 51838 terminated with exit 2 after 20 seconds:
  `native usage task baseline is ambiguous`. The new first `token_count`
  used a baseline equal to the root's 17:29 snapshot, rather than its latest
  23:49 counters. The host refused to infer a reset and recorded usage as
  unavailable/inconsistent. The 200M cap and medium setting remain saved.
  Read-only inspection of native `token_usage_record` metadata found a
  complete, additive per-response ledger in all 17 product threads. Its
  thread and turn totals match the sums of unique request records, including
  compaction requests. Its total is 102,371,792: the prior saved 100,142,201,
  plus 178,760 from the interrupted resume and 2,050,831 compaction tokens
  omitted by the existing notification-based reader. This is an audited
  observation, not yet a persisted corrected host charge. A validated ledger
  reader is being implemented with contract and failure-path tests; no native
  resume or manual budget-state edit is being used to bypass the ambiguity.
- 01:29:21 UTC: resumed again using the normal CLI with
  `--max-tokens 200000000 --effort medium --refresh-tools --strict`, selecting
  the same installed native Codex binary. CLI handle 93000 is live. The host
  refreshed exactly four CAD files: the motion reference, new assembly-copy
  helper, and mount/motion checkers. The exact native thread and runtime
  configuration hash are unchanged. Normal host reconciliation removed the
  nonterminal accounting need and persisted the corrected response ledger.
  New requests were successfully counted; status reached 102,555,377 tokens
  by 01:29:50, with medium retained and a 200M total cap. The other session's
  500M ceiling update is still absent. The native Manager reported that it is
  repairing remaining print-gate failures, reset flag/splice contact, and a
  mirrored canopy screw's catalog-pose mismatch. These are repair targets,
  not completed gates.
- 01:42 UTC: CLI remains live, with 112,188,636 observed tokens under the
  temporary 200M cap. Root `turn_context` records at 00:45, 01:29 and 01:33
  confirm medium. Three existing child sessions resumed at 01:33–01:34 still
  record ultra; this is an observed effort-propagation gap, despite the
  truthful root setting in the host receipt. Investigation is limited to
  supported native configuration and resume semantics; no native child state
  or frozen product configuration has been manually changed.
- Read-only controls review verifies 107 condition/report bindings, including
  45 clear paths and 62 blocked constraints, plus 43 one-solid support members.
  The original apron collisions remain preserved, and revised service paths
  require apron removal. Their recorded `params.py` hash differs from the
  current file, so final current-source reconciliation remains necessary.
  A separate mirrored-hardware probe recovers two proper rigid catalog poses;
  the complete mount manifest and verdict are still pending. Native repairs
  have widened the flag/splice relief and changed the return-hood nut-pocket
  roof; fresh checks are being produced and reviewed.
- Subsequent targeted reports pass the four repaired deck/foot overhang checks
  (`deck_0_1`, `deck_1_1`, `deck_1_2`, and `foot_rear`). The landing ramp's
  report still fails with a 598.5 mm² unsupported region spanning 16.9 mm.
  Splice revision 2 records 14 source-support passes bound to current source
  and STEP bytes, plus two native condition passes with a verified log hash.
  The separate 183-condition native retention pass is explicitly bound to
  revision 1; it is not a whole-current-product verdict. Reset, front-end
  removal, and the 46-condition controls subset remain pending at this
  snapshot.
- 01:48 UTC: the next landing-ramp overhang report passes, as do the two
  related deck reports. The host still reports active Make and no publication;
  observed usage is 117,875,910 of 200,000,000 tokens. The 140-row canopy/apron
  hardware probe provides catalog-pose evidence, not a complete mount or
  whole-product verdict.
- Final usability review must address the documented play cycle. Under
  `product/README.md`'s loading/play/reset section, every hand-loaded launch
  removes and replaces four front-right roof screws. A drain returns the
  marble to the cup, followed by that same loading procedure. Jackpot reset
  additionally removes and replaces four rear-left screws, then lifts only
  the marble into the hopper. The containment handoff explicitly describes
  these as normal loading/reset access, not just initial assembly. No
  alternative without tools is documented. This is a concrete concern against
  the Wish's simple manual reset and replayable arcade experience; the draft
  remains unfinished and a collision pass alone would not settle it.
- 01:58:58 UTC: gracefully interrupted the uniquely verified Workshop CLI
  host to apply the corrected effort handoff. Handle 93000 exited 130 after
  native cleanup. The root thread and runtime configuration hash remained
  unchanged, and no accounting need was present. The CLI printed an unnecessary
  KeyboardInterrupt traceback; a presentation-only correction is being checked.
- 01:59:49 UTC: normal `resume wish-20260910-132933-a88b698e --effort medium
  --strict` launched live CLI handle 6738 through the same installed Codex
  binary. No tool refresh or budget change was requested. By 02:00:40,
  status showed active Make, medium for the Manager, 127,916,071 observed tokens,
  and the saved 200M cap. Publication remains uncreated. Subsequent child
  effort still needs actual native metadata; the revised notice is not proof
  of changed child settings.
- Verified after that resume: the root's 01:59:53 context is medium, and two
  new directly bound native children began at 02:00:50 and 02:01:06 with
  medium contexts. No old ultra child began a fresh turn in that observation
  window. The Manager explicitly acknowledged the updated choice. The
  corrected handoff is therefore working in observed native execution;
  receipts alone still do not certify every future child request.

Final acceptance must inspect the exact complete assembly and both mission
layouts, independently operating flippers and band plunger, marble-driven
flag and complete drain/reset/reload cycle, current integrated engineering
and visual evidence, all print/source/STEP downloads, and a practical assembly
and play handoff. The exact Spark Wish uses the existing `product/README.md`
for that handoff; it does not add a `MANUAL.pdf` requirement. Publication is
complete only after the authenticated host receipt and exact public asset
readback identify Dee. Physical commissioning is subsequent Operations,
not evidence already supplied by this digital build.

Current recorded outcome: Make remains unfinished. There is no completed product,
passing final review, or publication receipt yet.

- 02:09:20 UTC: the native Manager started full Make round r0005 with the
  normal `make_round` CLI and `--render-timeout 3600`. This is the first
  observed live use of the timeout option. The CAD project remains native
  owned and frozen for the round; observer/toolkit work does not rebuild it.
- Fresh reset-path evidence passes 401 samples against 489 obstacles with
  no failures or source changes. A separate read-only comparison at 02:20
  confirmed that all 138 bound source files matched current bytes. This
  proves the declared marble extraction path in its service state, not
  physical hand access, rolling behavior, or ease of repeated play.
- Front-end service r003 passes 14 declared conditions with 512 source
  leaves, zero source changes, and a maximum 0.25 mm sample step. Its ordered
  removal sequence explicitly identifies 96 removed members, 416 remaining
  members, and required hand support. This is scoped service evidence,
  not the complete final motion or mounting gate.
- The current native stationary audit is substantially faster after adopting
  detached assembly copies. For 43 matched passing paths, saved elapsed times
  sum to 6,660.31 seconds before versus 212.38 seconds after; all report fields
  other than elapsed time match. One identical 144-sample strap-bolt path
  dropped from 169.1834 to 7.5760 seconds. These are observed run timings,
  not an isolated benchmark or a claim that the complete audit has passed.
- 02:33 UTC: the normal CLI remains live in Make at medium, with 147,802,547
  observed tokens of the temporary 200M cap. The shared source still limits
  budgets to 200M; the other session owns the requested 500M change.
  No Factory publication has been created.
- The first current hero render, written at 02:29:53 UTC, is unusable.
  Native public commentary and the observer's actual image inspection agree:
  sparse disconnected lines and large triangles replace the solid assembly.
  The native Manager rejected it for review and is investigating. A source
  tool diagnosis is running independently; the symptom alone does not prove
  a CAD geometry defect. The r0005 visual packet and full motion gate remain
  in progress, and final review/verification are incomplete.
- 02:40 UTC: read-only aggregation of all 142 r0005 print reports confirms
  71 thickness passes and 71 overhang passes, with no failed or unknown rows.
  The round is not complete: visual rendering and its native feedback remain
  pending. The native Manager independently traced the broken hero to
  `render_product` discarding triangles and started an existing faithful
  renderer path; no observer feedback was injected into the product session.
- By 02:48 UTC, r0005 completed its three-view render packet with deterministic
  checks passing. Native visual feedback was still pending, so the round's
  overall result remained false. The observer opened the actual full assembly
  isometric image; it shows the outer covers obscuring the internal playfield.
  This replaces the earlier partial-playfield preview for complete-assembly
  inspection but is not final presentation approval.
- The completed 500M change was found in the other session's local
  `make/mixed-material-products` branch, not on origin/main. Commit `df727a0b`
  was cherry-picked unchanged as `7faadd1d`. It preserves the 30M default and
  saved limits; explicit resume changes the total cap without resetting usage.
  Compatibility tests passed 185 cases. Commit `792fd980` corrects the two
  stale public README ceiling statements; historical 200M records remain.
- 02:52:09 UTC: the exact live CLI received a normal SIGINT and exited 130.
  Read-only process inspection found detached native-started CAD jobs outside
  the launcher session. The production exporter and an older service job
  completed before cleanup; two remaining, identity-verified job groups
  (the full motion checker and alternate presentation helper) received SIGINT.
  A subsequent process inventory confirmed no remaining process in this
  toy workspace before refresh. Interrupted jobs are not claimed as passes.
- 02:55:51 UTC: normal CLI resumed the exact root session with
  `--max-tokens 500000000 --effort medium --refresh-tools --strict`. The host
  rebound exactly the four audited changed files: STEP-package generation,
  both renderers, and Make-round's ordinary-use guidance. All 165 previously
  carried tool inputs matched their frozen hashes before refresh; the five
  allowlisted lifecycle-review files were unchanged. No toy source or proof
  artifact was edited by the observer.
- 02:56 UTC: fresh status verifies active Make, medium, and 159,048,726
  observed tokens out of 500,000,000. Root thread, runtime configuration hash,
  and product-run permission profile match the prior session. Publication
  remains not-created. Current exact command is also saved locally in the
  ignored `.tmp/cratercade/resume-command.sh`.
- The native root's 02:55:54 turn metadata independently confirms Astra at
  medium. Subsequent Make feedback records r0005 as inconclusive: source
  alpha 0.18 on clear PET panels is rendered opaque, hiding the interior.
  Print checks pass, but this packet does not establish interior placement
  or the required visible action. Native presentation recovery owns the toy
  work; separate source-tool diagnosis uses synthetic fixtures only.
- 03:00 UTC delivery audit: all 512 declared individual STEP files match
  their saved hashes, as do the combined assembly and its sidecar. The
  sidecar's STEP binding matches the delivered assembly. All 131 source files
  bound by that delivery report still match current bytes, with no extra source
  files in its declared scope. This verifies the complete export's byte
  consistency, not final Make acceptance or physical performance.
- 03:03:49 UTC: the native session completed its turn with an explicit Make
  need for host-provided transparency support; the CLI exited 1. Observed
  usage is 161,485,983 of 500,000,000 at medium. Native evidence proves that
  alpha 0, 0.18, and 1 currently produce identical opaque images, and that
  the motion validator reconstructs those same opaque bytes. A separately
  authored transparent hero cannot substitute for the required bound motion
  animation. The native Manager preserved the full enclosure and unfinished
  Make Goal instead of claiming readiness. Its second full-motion attempt
  was interrupted without a verdict; neither full-motion attempt is a pass.
- Repository work is addressing the blocker in both `render_review` and
  `motion_states`, with synthetic layered-scene and actual GIF reconciliation
  tests. The paused toy's frozen files remain unchanged until reviewed code
  is committed and adopted through normal host refresh. A separate runtime
  cleanup investigation is isolated in `/private/tmp/workshop-native-cleanup-fix`
  so it cannot delay a safe transparency-refresh resume in this checkout.
- 03:28:57 UTC: resumed the same native session through the normal CLI with
  `--max-tokens 500000000 --effort medium --refresh-tools --strict` after
  committing transparency fix `aad0672f`. The pre-refresh audit verified all
  165 carried inputs and identified exactly four changed CAD files: the review
  renderer, motion-state material handling, and their two references. The host
  receipt confirms those four rebindings; no lifecycle review files changed.
  A fresh process inventory found no accessible process in the paused toy
  workspace before refresh. Astra/medium is independently confirmed by the
  new root turn metadata. Make is active; publication remains not-created.
- 03:29 UTC: observed usage is 161,561,966 of 500,000,000. The native Manager
  is rerunning its own bounded transparency probe before resuming review work.
- The native alpha probe passed with the refreshed hashes and recorded
  `prior_need_resolved: true` in `design/alpha-refresh-reconciliation.json`.
  It started review round r0006, full native motion r4, and canonical motion
  presentation r1. Motion r3 failed only because of a relative manifest path
  before geometry; r4 corrected that invocation.
- 03:34:10 UTC: Make returned a new explicit Need and the CLI exited 1.
  The native pre-review audit proved that the required verifier accepts exact
  review schema 7 while the required finalizer accepts exact schema 8. The
  Manager interrupted r6, motion r4 and animation r1 after proving that host
  incompatibility; none of these interrupted attempts has a passing verdict.
  Product geometry, completed production files, caches and unfinished Goal
  remain preserved. Transparency is resolved and is not the current blocker.
  Usage is 164,006,182 of 500,000,000 at medium. Source repair is in progress.
- The separately reviewed runtime cleanup fix was cherry-picked as `f9fc1d4e`
  after the run had already stopped. It is available to the next CLI launch;
  the last run used the previous cleanup implementation. Its isolated original
  commit is `17208320`.
- 03:49 UTC: resumed the same native session with the exact saved CLI command,
  medium effort and the 500,000,000-token budget, after compatibility commit
  `4dfb4e68`. All 165 carried inputs matched their frozen hashes and modes;
  exactly eight CAD files changed: the skill, two references, two print-report
  writers, verifier, motion presentation and motion-state encoder. The host
  receipt confirms eight rebindings. The five allowlisted lifecycle files
  remained unchanged. Fresh pre-refresh process inspection found no accessible
  process in the paused toy workspace. This CLI launch also loads the reviewed
  runtime cleanup fix. Root session ID, runtime configuration hash and product
  permission profile remain unchanged. Make is active; publication remains
  not-created. At 03:49:21 usage is 164,102,621 of 500,000,000.
- Compatibility validation: 128 schema/native-CAD/finalizer tests, 83 motion
  tests and 24 registry/packaging tests passed; verifier self-checks and all
  six skill locks pass. Both independent reviews and root review cleared the
  13-file bundle. CAD fingerprint is
  `fd34d02e55703480898de9652f4976dafaa60d5a31387403d1200939f4f0aeab`.
- The native root's 03:49:09 metadata confirms Astra/medium, and its fresh
  schema probe confirms both mandatory gates now require schema 8. The current
  processes are full motion r5, canonical animation r2, review-round invocation
  r7, and the canonical print sweep. The round invocation reuses unfinished
  `measure/rounds/r0006`; its log suffix is not a new round-directory identity.
- 04:00:57 UTC: 97 canonical thickness/overhang reports contain passing results;
  the full expected set is 142. Make remains active at 169,040,812 tokens of
  500,000,000. Both exact jackpot STEP states have tessellated all 512
  occurrences. Their completed endpoint evidence records 3,328,146 facets
  in each state; the earlier complete assembly count was 3,328,154. The first transparent
  image, `design/signature-transparent-r1/state-0.png`, is now written. Root
  opened it in Preview and inspected it inline: the interior is visible
  through the complete enclosure. This is one current CAD view; the animation,
  second state, complete motion verdict, and final review remain outstanding.
- 04:02:40 UTC: both wide endpoint images completed, but their measured mean
  RGB difference at 96x96 is 0.22468 against the unchanged minimum 2.0. The
  wide comparison therefore fails the visibility check. Native Make is
  preparing one fixed close-up framing from the same exact states, retaining
  all assembly geometry and the threshold. No image pair has been accepted as
  final signature evidence yet.
- 04:05:46 UTC: all 142 canonical thickness/overhang reports contain passing
  results, covering the 71 printable entries. Native Make wrote
  `design/final-print-report-hashes.json`; root independently checked all 142
  saved digests against the actual report bytes, with zero mismatches. This
  completes that report-preparation sweep, not the final integrated verifier
  or Make gate. The full motion result and visual/animation reviews remain
  outstanding.
- Native Make subsequently reported all 213 canonical mesh/thickness/overhang
  checks passing. Review-round invocation r7 completed its numerical portion
  as round 6 with `checks_ok: true`; `ok: false` still reflects pending native
  inspection of its three new transparent views. Root inspected the top view.
  This round deliberately omits motion; full native motion r5 remains separate.
- The fixed close-up encountered an offscreen-triangle renderer crash. Native
  Make independently added a viewport prepass to its presentation helper and
  tested it against a larger-render crop byte for byte. It retains the original
  frozen renderer and CAD geometry and is retrying as `signature-closeup-r2`.
  Root's separately reviewed source correction `048f6ee5` is committed but
  **not materialized into the live run**. Do not interrupt active jobs merely
  to install it if the native presentation workaround succeeds.
- `signature-closeup-r2` completed and passes its unchanged visibility
  threshold: mean RGB difference 2.81308 versus minimum 2.0. Its fixed framing
  is [118,291,86] to [199,419,185] mm; all 512 occurrences remain in the scene,
  with only faces having no possible image pixels omitted by the viewport
  prepass. Root inspected the side-by-side image and can see the flag rise.
  This is candidate visual evidence, not independent review or physical motion
  proof. The native root also began assessing ordinary roof access and reset
  against the Wish's simple manual reset requirement while the long jobs run.
- 04:16:27 UTC: native Make confirmed a design defect in that ordinary cycle:
  every launch requires removing/refitting four roof screws, and jackpot reset
  requires another screwed-down cell. It judged this incompatible with the
  requested simple reset and is replacing both access points with captive,
  hand-operated doors while preserving the enclosed playfield and jackpot
  mechanism. This is native product repair, not observer CAD editing.
  Full motion r5 and canonical animation r2 were interrupted without passing
  verdicts before repair. The 213 passing checks and 142 hash-matched reports
  describe the preceding source state; changed parts and final evidence must
  be revalidated after the door repair. At 04:19:05 Make remains active with
  175,187,832 observed tokens out of 500,000,000.
- 04:27 UTC: the door design handoff is complete and native implementation is
  underway. Bob owns the door parts; the root Manager integrates independent
  motion states while preserving the fixed roof mounts. Host status confirms
  Astra medium, the 500,000,000-token limit, and 177,067,512 observed tokens
  (completed root/descendant requests, excluding in-flight usage). Release
  publication remains not-created.
- 04:30:31 UTC: native Make identified a possible additional reset-access
  defect: the captured marble lies below the bucket side rims, potentially
  preventing a fingertip grasp. It is checking the exact tipped geometry;
  this is not yet a confirmed handling repair. Initial door-part construction
  and local geometry/print checks are underway, with support and clearance
  failures still being resolved. A separate read-only builder audit found
  no instruction conflict: Make-round already requires ordinary-use review
  before expensive verification, and formal final blind review is a later
  second check. No redundant workflow instruction was added.
- 04:35 UTC: revised receiver and frame local overhang reports now pass.
  The door's local prescribed-pose check reports no intersections, with
  positive interference at open overtravel and locked-turn stops. These checks
  concern the local door assembly; complete-product neighbours, current exact
  report bindings and independent motions remain to be qualified. Bob is
  specifying bucket rim relief after the fingertip probes found only upper
  pinches that would rely heavily on friction. No bucket CAD edit is present
  in the inspected handoff yet.
- 04:40:59 UTC: the first integrated source overlap check correctly failed
  the repaired door assembly. Native Make identified missing moving-screw
  recesses in the fixed receiver, nut-pocket alignment, and frame bridge
  overlaps with the cap/PET. Bob is repairing those exact interfaces; local
  passing checks did not cover them. The current integrated inventory has
  542 occurrences. Root Manager has implemented rounded bucket finger relief
  and is checking both permitted marble sizes, grip clearance and retained
  lower lips. No final product gate pass is claimed.
- 04:45 UTC: integrated door static check r2 passes its declared 283 candidate
  pairs in the 542-occurrence assembly. New exact-cell hardware/opening and
  latch checks also report no collisions; complete-product movement remains
  separate. Bucket grip r2 passes local tangent finger clearance for 11 sampled
  marble sizes. Both permitted size limits have blocked level side exits;
  the first clear sampled crossing requires 4.3 mm lift for the 15.5 mm ball
  and 3.9 mm for the 16.5 mm ball. This is not incoming-ball capture dynamics
  or physical grip proof. Jackpot balance r1 failed after material removal;
  normal adjustment of the existing trim weight produces passing r2, with
  modeled empty restoring torque 0.256–0.307 mN·m and light-marble minimum
  tipping torque 0.717 mN·m under the existing mass assumptions. Full paths,
  exact final geometry/report bindings and physical commissioning remain.
- 04:49 UTC: full supplied hand-path r1 reports finish with failures and
  unchanged source hashes. Loading has 149 failing poses among 757 samples;
  reset has 176 among 1,332. Both retain 541 obstacles from the 542-occurrence
  assembly and use bounding-box pruning before exact intersections. Recorded
  contacts include canopy/receiver interference during loading and bucket
  interference during reset. No complete hand-path pass is claimed. Native
  Make is also rebuilding the hardware inventory from the repaired assembly,
  replacing the prior 512-occurrence bindings.
- 04:51:07 UTC: native Make traced the full-path failures to a ridge above
  the finger relief, interference during bucket return, and a loading path
  brushing the front roof support. It also found the launcher crossbar
  extending beyond the strike face. Repairs now extend the relief upward,
  revise the supplied handling paths and make the strike face flush. These
  new changes require fresh affected geometry, print and balance evidence;
  the earlier local passes are not final evidence for this revision.
- Full hand-path r2 results for the 16.5 mm marble: loading passes all 754 sampled poses in the
  542-occurrence assembly; reset fails 14 of 1,337 samples during the start
  of bucket return. Both reports verify unchanged source during their runs.
  The latest relief's balance r3 also passes its existing seven checks.
  These results improve the local design but do not complete the full reset
  cycle or final integrated product verification.
- 04:59:37 UTC: after checking the saved r2 failures, native Make is evaluating
  a relief change that clears early bucket return without relying on precise
  hand timing. It is also completing release into the return hopper. The same
  native session continues; no new Make attempt or operator restart occurred.
- 05:02:42 UTC: both full hopper-release/finger-withdrawal checks now pass
  (235 samples for 16.5 mm and 237 for 15.5 mm), with stable source bindings.
  Seven focused launcher conditions also pass: stroke, both end stops,
  directional retention and service lift. Their report explicitly excludes
  complete native motion verification and physical force/friction performance.
  The smaller-marble loading r2 fails 78 of 753 poses against the door
  receiver; native Make adjusted outward finger movement for its next path.
  Bob's accepted reset repair preserves the bucket floor and lower lip while
  removing the remaining early-return finger interference.
- 05:10:35 UTC: native Make found that the receiver's hinge nut cages require
  a distinct mirrored loading-receiver print file. It added
  `part_access_receiver_load.step.py`; the other five door part types pass its
  rotation-based congruence check. Both full reset r3 jobs started at 05:11:48.
  Local grip r3 and balance r4 pass after the new return-clearance relief;
  full reset path results and refreshed final print evidence remain pending.
- 05:15 UTC inspection: both complete supplied reset r3 paths pass against the
  542-occurrence assembly, with no failures and unchanged source during each
  check: 1,575 samples for the 15.5 mm marble and 1,571 for 16.5 mm. These are
  finite geometric paths, not physical grip or timing tests. Native Make has
  frozen source for loading rechecks and refreshed inventory, including the
  distinct receiver. Host status at 05:15:44 remains active Make, Astra medium,
  215,368,198 observed tokens out of 500,000,000; publication is not-created.
- 05:18:03 UTC: fresh inventory reports 542 occurrences, including 398
  fasteners, and 78 printable entry types. Native mounting and front-end
  service checks and the next Make print/visual round are running; native
  Make explicitly froze CAD/STEP until its inspection is recorded. Both
  loading r3 paths fail later in withdrawal (90 of 753 samples for 15.5 mm,
  86 of 754 for 16.5 mm), with stable source. The earlier larger-marble loading
  pass does not establish that this revised path passes. Correcting the
  supplied path remains separate from the frozen geometry and broader gates.
- 05:21 UTC: the native mounting report passes all 398 measurements with
  zero findings. Reset at the middle 16 mm size also passes 1,573 samples
  with stable source, joining the two endpoint-size passes. Updated
  `creative-source.json` covers all 542 occurrences in 57 source families;
  its build first caught a stale component mating reference, which native
  Make repaired before successful validation.
- Current round-seven reports expose thickness failures in both access
  receiver variants (reported minima 0.43 mm) and the jackpot rocker
  (0.34 mm). The nine overhang reports currently written pass; six current
  thickness reports pass and three fail. This is a partial-round observation,
  not a complete Make-round verdict. Full-reset and earlier local passes
  describe this pre-repair geometry and need appropriate revalidation after
  these upcoming changes. Render/inspection evidence is still pending.
- Loading r4 now passes at all three tested diameters with stable source:
  858 poses for 15.5 mm and 857 each for 16 and 16.5 mm. The supplied path
  brings the fingers toward the opening centre before lifting them clear.
  This completes the currently tested loading/reset path family; the three
  print-thickness failures and pending broad motion/visual work remain, and
  upcoming geometry repairs require fresh affected evidence.
- Round seven is complete with `checks_ok: false` and `ok: false`: nine
  changed printable types were checked and 69 unchanged types reused valid
  prior checks, covering all 78 entries. Print logs use a 0.80 mm wall threshold
  with their recorded measurement uncertainty, a 0.4 mm nozzle and a 45-degree
  overhang gate. The summary's `min: 0.9` is the likeness floor, not the wall
  threshold. All three rendered
  views exist; the exact visual packet still awaits native Manager inspection.
  Root displayed the current top view, which visibly includes the new exterior
  door hinges. The native Manager also began checking front collection-cup
  pickup as an additional ordinary-use reach question; no result is assumed.
- 05:32:56 UTC: native Manager recorded round-seven visual PASS against
  packet `859fa9f09f1a264c6fc35e9dedbbb603927e522aa334ac9b3314eedef5d4fc9a`.
  Numeric failures remained. The first revised cup passed local finger checks
  but the full assembly still put larger seated marbles into the return hood.
  Old-source captive-door and front-end service jobs were stopped with explicit
  interrupted-stale records and no completed condition or passing verdict.
- 05:50 UTC: round-eight visual review passed after all three images were
  inspected. One receiver thickness repair passes; the handed loading
  receiver still reports 0.43 mm, the collection cup 0.26 mm and the rocker
  0.34 mm, each failing its unchanged thickness check. Root viewed its iso
  image; complete canopy, opposite-side hinges, controls and central flag are
  visible. These images do not establish printability or hand access.
- 05:52 UTC: native Make implemented the 5 mm cup-front extension and
  through-wall openings, plus receiver and rocker remnant removal. The
  receiver's exact subtraction check preserves the floor and eight protected
  interfaces. Local cup checks pass for five marble sizes, retaining the
  floor/lips and an unobstructed 80 mm pickup sweep; separate recentering
  checks report no failures. The rocker repair removes 20.31 mm3 and retains
  one valid solid. These are bounded repair checks, not the next print round
  or current complete-assembly motion verdict. Fresh balance, combined
  pickup/loading, ball-arrival paths and final integrated evidence remain.
- 05:58 UTC inspection: the combined cup-pickup/carry/loading r5 path passes
  at 15.5, 16 and 16.5 mm (2,372, 2,370 and 2,368 poses). Reset r3 reruns also
  pass (1,575, 1,573 and 1,571 poses). Each report includes 542 occurrences,
  zero failures, stable source and 148 source bindings; the observer verified
  all those bindings still match current files. Full-assembly one-finger
  recentering passes 450 poses across five sizes plus ten continuous approach
  sweeps. Updated modeled rocker balance r5 passes with empty restoring
  torque 0.312447–0.377506 mN·m and minimum loaded tipping torque 0.660327 mN·m,
  under its stated assumed densities and exclusion of friction.
- The new arrival assessment has a split result: all five prescribed terminal
  rolloff-to-cup paths clear, but every proposed drain-to-cup path crosses the
  return hood/floor. `approved_all_clear` is false. This establishes a failed
  supplied geometric route, not yet whether the required repair is a better
  path or changed geometry. The result remains separate from passing hand
  paths and does not support a complete replay or dynamic capture claim.
- 06:04 UTC inspection: native Make reports that the direct drained-marble
  route hits the return channel and is assessing an east-side path with a
  small cup-width adjustment. The current merged `complete-motion-r3.json`
  and its binding evidence exist, but are candidates without a current
  complete native geometry verdict. The canonical print-report refresh r2
  handoff is explicitly prepared-only: its helper has not executed any gate
  or regenerated any report. It plans per-role hash-qualified reuse and
  native regeneration after round-nine visual feedback; this is not a new
  print pass. CLI 74939 remains live, and no tool refresh has occurred.
- 06:07:17 UTC: CLI 74939 exited 2 after the native account usage-limit error.
  The product rollout's terminal event names `usage_limit_exceeded`; the
  host's saved bounded diagnostic instead says `unclassified`, with a
  140-byte message and successful process cleanup. Read-only projection of
  the observed message reproduces that classification gap. The current
  handoff preserves all geometry/results and the unresolved east-side drain
  route; its proposed 1 mm cup-side extension has not been approved or built.
  No round nine, final review, integrated verifier or publication has passed.
- 08:20:59 UTC: after the user reported restored Codex capacity, the ordinary
  CLI resumed the exact saved session with medium effort and a 500M limit.
  A fresh pre-resume audit verified all 243 frozen inputs and exactly four
  allowed tool updates with no lifecycle or mode drift; the host rebound
  those four files through `--refresh-tools`. The first sandboxed invocation
  could not open the private state lock and made no run mutation. The same
  authorized command with filesystem access succeeded as CLI handle 88872.
  Native turn metadata at 08:21:02 confirms Astra medium; fresh commentary
  and tool calls confirm actual recovery, not merely an active saved status.
- 08:29 UTC inspection: the resumed Manager preserves the same Make subject,
  Goal and Bob assignment. Its design assessment reports that a proposed
  1 mm east-side cup widening still catches the largest marble. An in-memory
  4 mm widening has clear prescribed routes for five sizes (447 poses each),
  but no guide yet establishes how a drained marble enters that corridor.
  This remains candidate geometry rather than a finished drain mechanism or
  a new CAD/print pass. Print preparation has qualified exact evidence reuse
  for 68 unchanged part types; ten new or changed types await fresh native
  checks after the cup repair. CLI 88872 remains live; round nine is not yet
  complete and publication has not started.
- 08:34:34 UTC: native Manager read and accepted the completed drain handoff.
  It specifies outer east wall X186/inner X183, right pickup cutter through
  X187, and lane X174.25. The exact candidate clears all five sizes in
  428–429 samples, with 0.50 mm nominal wall clearance and 0.55512 mm clearance
  to the hood/floor maximum-X plane. The existing northeast hood boundary
  supplies a southeast tangent route whose board Y decreases throughout;
  no new deflector is needed for this static geometric route. Passive branch
  selection, friction and settling remain physical commissioning questions.
  Source construction followed at 08:36:10; wider-edge recentering and
  source-bound evidence updates remain pending before the next full round.
- 08:43 UTC inspection: fresh inventory r6 completes with 542 occurrences and
  398 fasteners; the mount manifest hash remains `5667ee6f…b62d4`. This is
  static inventory qualification, not a new native mounting gate pass.
  Pickup-to-loading r5 reruns against widened-cup source pass for 15.5, 16 and
  16.5 mm marbles (2,372/2,370/2,368 samples), with zero failures and stable
  source. The observer verified all 148 bindings in each report still match
  current CAD source. Remaining wider-edge/access work and the next full
  print/visual round are pending; CLI 88872 remains live.
- 08:53 UTC inspection: all six widened-cup loading/reset reports pass with
  stable source and zero failures. Reset counts are 1,575/1,573/1,571 for
  15.5/16/16.5 mm marbles. The observer independently verified all 148 source
  bindings in every report. `collection-east-repair-r3/report.json` now passes
  and binds the current cup source `117043fc…9c3e`. Its first failed assertion
  is preserved separately: the checker had incorrectly required zero removed
  material while translating the wall; native Make corrected it to check the
  intended removed and added regions. This is native product evidence repair,
  not a Workshop source fix or observer intervention. Native Make reports the
  wider-edge recentering/pickup checks passing and starts round nine at
  08:51:55. The motion proposal contains 1,303 conditions and six coupled
  tables; generating it does not itself pass the native motion gate. Host
  status records 276,946,436/500,000,000 tokens and publication not-created.
  Opened round eight's isometric image for the user; it predates cup changes.
  User-facing working ETA is another 2–4 hours if remaining checks pass,
  explicitly conditional on no further mechanical failures.
- 08:56 UTC independent read-only audit confirms remaining work: round-nine
  verdict/three-view feedback, the full 1,303-condition motion verdict,
  canonical print evidence reconciliation, final stills/animation and review,
  integrated verification, current STEP/delivery reconciliation, Made and
  associated stage contracts, then host publication with public readback.
  The older delivery describes 512 occurrences rather than the current 542.
  Spark's frozen protocol needs no separate Playtest, native Release turn,
  duplicate host CAD pass or new PDF manual. No product files or processes
  were modified by the audit.
- 09:03 UTC inspection: round nine has 101 print-check Markdown reports and
  no completed summary or views yet. The loading receiver, collection cup
  and jackpot rocker all now report printable thickness and unsupported
  printing; these are the three roles that failed in round eight. No FAIL
  marker appears in the reports written so far. The full native motion
  report remains empty while CLI 88872 is live, so neither the round nor the
  motion gate is claimed to pass. Preview accepted an explicit render-open
  request, but its subsequent AppleEvent window query timed out; the window
  was not visually confirmed. The user was given the direct render link.
- 09:04 UTC independent cache audit: round nine correctly reruns all 78
  print roles because Make-round's exact print context includes its wrapper
  hash, which changed from `4118370b…` to `0baa7664…` during the audited
  failed-part-index refresh. Numerical gates and their two libraries are
  unchanged, but the wrapper also controls measurement arguments and result
  parsing. Removing this provenance check is not justified by this incident.
  The product's separate 10-refresh/68-reuse canonical-report plan does not
  control Make-round's cache. At 09:03:54 there were 118 fresh reports
  (59 thickness/59 overhang), with no completed round summary. No source
  change or live-run intervention was made for this expected invalidation.
- 09:08 UTC inspection: all 156 round-nine print reports are present and
  contain no FAIL markers. Native Make independently acknowledges their
  completion while explicitly awaiting the consolidated round result and
  visual packet. No round-nine views or summary are present yet, and the
  full native motion output is still zero bytes. CLI handle 88872 remains
  live; host status at 09:07:59 records 282,298,471 tokens and publication
  not-created. Current activity is a verified wait on the same run, without
  restart, tool refresh or artifact intervention.
- 09:17 UTC inspection: round nine is now `checks_ok=true`, `ok=true` after
  native Manager inspection of all three exact views. Packet hash is
  `5bdd3fbf92740d59f443dd2752b7d68409f483c5751502eaf38a7adc7fb4935d`.
  Native feedback reports no visible placement or intersection errors. The
  source remained frozen through feedback. The canonical refresh log also
  reports `passed=true`, refresh ten/reuse 68/failures zero, covering 30 fresh
  mesh/overhang/thickness checks. The native Manager is regenerating the
  combined assembly and delivery package next; full motion remains active
  without a verdict. Displayed the updated front and isometric views and gave
  the user a direct link to the new isometric file.
- A separate builder improvement was identified: opt-in, rate-limited stderr
  progress for `check_motion`. The current frozen verifier emits nothing
  until all work completes; its exact invocation at 08:52:41 redirects stdout
  to `complete-motion-r4-native.json` and stderr to its `.stderr.log`, both
  still empty during the ongoing check. This is absent instrumentation, not
  evidence of a hang. The intended source change preserves stdout, evaluation
  order, thresholds, verdicts and exit behavior; it will not be injected into
  the current invocation or used to justify restarting it.
- 09:25 UTC inspection: the combined assembly export succeeded, and native
  Make is refreshing its package and the two jackpot endpoint states for a
  closer signature view. The motion gate still has no verdict; CLI 88872 is
  live after more than an hour, with 288,454,943/500,000,000 tokens recorded.
  Builder commit `88d9f9a8` completes optional motion progress with 123 focused
  and regression tests, 24 registry/package checks and both self-check suites
  passing. Root and independent review found no blockers. Only two frozen
  skill files would change on refresh: CAD `scripts/check_motion` and
  `references/motion-manifests.md`. These are optional updates; do not restart
  or refresh this run for them. A necessary later resume should preserve
  frozen tools unless an actual blocking fix justifies a refresh.
- 09:32 UTC inspection: the regenerated assembly package matches all 542
  current occurrences. Delivery STEP and closer jackpot rendering continue.
  The movie reader rejected the 49,000,077-byte formatted motion manifest;
  native Make preserved it and compacted the canonical file to 9,124,598
  bytes. Root independently compared the parsed JSON and confirmed exact
  equality to `design/complete-motion-r4.json`, the input of the ongoing
  collision check. Canonical compact hash is
  `1bc54bbe20bf9dda9e6dc6f2603650967e694920083a28131f1042feccc09164`;
  unchanged candidate hash is
  `dba5f8c5f263a670edc028f9989e45386ecff3e04711892ce2ef6a2d7d434658`.
  Movie generation accepted the compact input and began at 09:31:27. No
  conditions, poses or reader limits changed, and no observer feedback or
  product edits were involved. Full motion remains without a verdict while
  CLI 88872 is live; publication remains not-created.
- 09:37 UTC inspection: production STEP files continue to appear and the
  canonical hero exists at `product/cad/snap/iso.png`. Native Make rejects
  the first close-up pair as visually insufficient even though its numeric
  image-difference check passes: a canopy post hides the nearly edge-on
  flag. It is rerendering the same geometry from a lower three-quarter view.
  Its remaining-check audit identifies fit, mounting, return-clearance and
  static-interference reports for fresh current-source checks, while print
  and band-sweep evidence remains reusable. No full motion verdict exists
  yet. CLI 88872 remains live with 294,768,519 tokens recorded; no publication
  or physical-test claim is made.
- 09:43 UTC inspection: fresh local fit passes all 151 checks (142 connector
  ledger entries), and return clearance passes all 414 samples for the
  16.5 mm marble. The latter covers source free space against return parts
  and hardware, not a predicted trajectory or physical test. Root verified
  all 148 recorded source hashes remain current. The native report preserves
  an initial incorrect script-path invocation and its successful correction;
  no source-tool change was needed. The next close-up pair is also rejected:
  the raised flag is clearer but the tipped bucket is hidden by the frame;
  its numeric difference is 1.76624 and fails. Native Make is trying a
  frontward camera below the roof rail with unchanged geometry/poses.
  Delivery export, the camera view, movie generation and full motion remain
  active. No motion verdict or publication exists yet; CLI 88872 remains
  live at 297,005,186/500,000,000 tokens.
- 09:57 UTC inspection: strict bed fit passes all 78 types at 200 × 200 mm,
  while the 398-mount gate reports one seat clash at `flipper_guard_4_nut`,
  4.0753 mm³ (6.2% of the component). The source bindings stayed stable during
  those checks. Native Make preserved the failure and interrupted motion,
  movie and delivery jobs with exit 130 and no completion verdict. It is
  diagnosing the cup exterior and checking static interference in both
  missions before geometry repair. Camera previews continue as diagnostics.
  Root requested that Preview open round nine's isometric image; macOS
  accepted the request, but the UI tool failed to start, so window visibility
  could not be verified. The same image was displayed inline. CLI 88872 is
  live at 305,626,088/500,000,000 tokens; publication remains not-created.

- 10:05 UTC inspection: the local cup notch is implemented and its bounded
  report passes with a matching current source hash. It removes 14.9695 mm³,
  adds nothing, clears the exact nut by 0.25 mm and preserves the protected
  working surfaces. Round ten has started; no verdict yet. Earlier full
  motion, movie and delivery jobs remain interrupted without completion.
  Root gave an updated rough ETA of 3–5 hours with low confidence, reflecting
  the new repair and remaining verification, presentation and publication.
- 10:10 UTC inspection: `design/check-mount-r7.json` passes all 398 mounts
  with zero findings. All 148 source hashes in the task's pre-check snapshot
  still match current CAD. Its combined mount/static summary remains running.
  Round ten has generated all 78 entries and its two fresh cup print reports
  pass; the round's final summary and images are pending. The refreshed
  modeled balance report also passes, without physical-test claims. CLI
  88872 remains live at 319,670,039/500,000,000 tokens.
- 10:14 UTC inspection: all six current loading/reset reports pass, with
  all 148 source bindings in each independently verified. Both complete
  mission assemblies now have zero source overlaps (1,815/1,819 candidate
  bounding-box pairs). The mount/static summary passes with stable before/
  after source hashes and binds all three current gate reports. Round ten's
  front render is present; top/isometric and the overall summary remain
  pending. CLI 88872 is live at 323,551,406/500,000,000 tokens.
- 10:22 UTC inspection: round ten passes numeric and visual review with one
  freshly checked cup and 77 exact reused types. The native full-motion check
  restarted at 10:18:34 against repaired CAD; its output has no verdict yet.
  Canonical print refresh and combined STEP export completed; endpoint export
  and the 48-frame movie started. Separately, native `inspect validate`
  reports 185 `selfIntersecting` nut occurrences (one positive-volume solid
  each), while `inspect interfere` passes with zero clashes. Native evidence
  preserves that failure and its source bindings. A builder source audit
  found no evident misclassification or transform omission; a synthetic-only
  reproduction and source-target freshness audit are pending. No tool fix,
  gate waiver or product mutation by the observer occurred. CLI 88872 remains
  live at 331,278,300/500,000,000 tokens; publication is not-created.
- 10:30 UTC inspection: native Make acknowledges the nut validation blocker.
  Its supplier nut passes unplaced, fails under the six-degree board tilt,
  and passes after inverse transformation; a representation repair is under
  investigation, with no accepted source change yet. Animation was stopped
  at 10:29 while full motion continues to collect other failures. Builder
  synthetic-only checks in `/private/tmp/workshop-synthetic-nut-validity/`
  found no general validator/rigid-transform or repeated-call defect: seven
  direct and seven scene poses pass, and invalid controls fail as expected.
  The separate freshness audit confirms explicit Python inspection generates
  fresh in-memory geometry and reloads first-party dependencies. The native
  summary's attribution to old viewer-package hashes is unsupported; those
  are separate disk observations, not inspector result bindings. Its native
  interference tolerance is 1 mm³, despite the summary's `tolerance_mm` label.
  No source/tool change or gate waiver was justified. CLI 88872 remains live
  at 336,200,506/500,000,000 tokens, with publication not-created.
- 10:32:39 UTC: CLI 88872 exited 2 after a native top-level `error`. Its saved
  diagnostic is unclassified, with no error code and a 39-byte message; no raw
  text was retained. The native rollout records an interrupted turn following
  host cleanup, not a cause. Cleanup passed and `timeout_seconds` is null.
- Before recovery, root verified all 243 frozen input hashes, sizes and modes,
  plus skill/agent inventories, with no drift against stable revision 11.
  Total frozen input size is 2,845,424 bytes. Optional source improvements
  were deliberately not refreshed.
- 11:03:57 UTC: ordinary CLI handle 32800 resumed the exact native session,
  medium effort and 500M cap with `--strict` and no `--refresh-tools`.
  Fresh native commentary/tool activity occurred at 11:04. At 11:06:15 it
  again exited 2 with a top-level unclassified 39-byte native error, no code,
  successful cleanup and no Workshop timeout. Usage is 336,453,385 tokens.
  No new resume has been launched. Root is investigating native error/reconnect
  semantics with an isolated credential-free fixture; connection failures in
  the separate observer session are not proof of the product error's cause.
  The previous rough ETA is no longer dependable while this failure persists.

## Pending checks against the actual CAD

A read-only review of Bob's construction specification identified these checks
for the eventual model. They are not findings about a completed build:

- The specified 22 mm under-deck return at X24 and Z-27..-7 crosses transverse
  seam straps at Y160/320. Literal strap dimensions occupy X20..140 and
  Z-12..-6 there. Verify that the actual CAD relieves or reroutes the overlap
  while preserving the ball passage and the deck joint.
- A straightforward 160 × 144 mm roof frame with 6 mm borders and 3 mm deep
  grooves accepts at most a 154 × 138 mm insert; the draft specifies
  156 × 140 mm. Verify the real cross-section and final sheet-cut dimensions.
- Check the practical replay loop: the proposed loading/reset method removes
  screw-retained roof cells. Final service states and instructions must show
  accessible loading, ball-only jackpot reset, and restored containment.
- Verify the loaded/empty rocker balance, all ball passages, independent
  control sweeps, and both actual mission layouts in the final geometry.
  Include the clamp hardware and declared print/mass assumptions in balance
  evidence, and check largest-ball contact without attaching the marble to
  the rocker for animation.
- The preliminary balance script prescribes 0/-28-degree endpoints; prove
  real stop contacts at both ends in final CAD. Its frame inspection did not
  yet show those endpoint stops. Positive empty torque alone does not define
  a ready-position stop.
- Reconcile print mass and trim adjustment with the solid-PLA calculation.
  Sensitivity arithmetic on its reported moments found that uniformly scaling
  printed-part mass to 65% raises maximum empty torque to 0.4074 mN·m; at 50%,
  minimum loaded tipping torque drops to 0.4615 mN·m. These are mass factors,
  not slicer infill percentages. Final print settings and measured-mass trim
  commissioning must support the chosen setup.
- The preliminary collision loop covers moving rocker/trim/hardware against
  the frame, axle, and ramp only. Include the free marble, canopy, hopper,
  mission obstacles, and fasteners in complete-assembly evidence. Regenerate
  balance evidence after final geometry changes: the preliminary JSON has no
  source/STEP hash binding.
- Confirm Mission B's guide/flag clearance and its exact bolt holes, both
  flippers acting separately and together, the retained 18 mm plunger stroke,
  and accessible guards, loading, and drain paths.
- Reconcile the complete parts inventory, craft cutting list, and assembly
  instructions with every actual CAD component. Bob's abbreviated build groups
  omit some listed caps, walls, and other components; completing those groups
  alone cannot prove a complete fabrication package.

## Issues found while acting as a CLI user

1. **Convenience runner rejects the requested ultra effort.** The installed
   main CLI supports Codex Astra ultra, but `run_wish.py` had a separate
   four-value argument/validation list. The actual product was launched through
   the main CLI with ultra unchanged. A source fix now shares the runtime's
   reasoning-level list and model/manager validation. Regression coverage
   checks Astra alias/full ID, unsupported combinations, system-Python runner
   entrypoints, and immutable resume settings. Focused validation passed 25
   tests across the runner and manager contracts. The reviewed fix is local
   commit `9aab2a39` (not pushed). This change does not alter
   Make, publication gates, or the live run's frozen settings.
2. **Local installation documentation names a missing executable.** The
   documented optional isolated installation does not exist on this machine.
   The installed native executable under the user's global npm package reports
   the required Codex 0.153.4 and passes `workshop doctor`. Documentation was
   corrected; runtime version checks and credential isolation were preserved.
3. **Optional host renderer is unavailable.** `workshop doctor` reports missing
   renderer dependencies and explicitly skips that enrichment. It still reports
   Workshop ready. Spark uses Make's existing imagery; this observation alone
   does not establish a Make rendering defect or justify modifying its gates.
4. **Transient usage discovery aborts a working run.** The token observer
   checks candidate session identities before selecting the product's native
   descendants. An incomplete first metadata record can therefore stop the
   CLI. The observed failure cleared on an unchanged read; the exact candidate
   is unknown. Local commit `8cd16049` adds at most three fresh metadata
   snapshots with 50 ms between attempts, only for incomplete first-line
   framing. It neither skips candidates nor invents usage. Persistent,
   malformed, duplicate, oversized, or linked records still fail closed.
   Focused usage, compaction, and token-budget tests passed 123 cases,
   including exact descendant charges and no double counting. This addresses
   a reproduced creation race; it does not prove the exact live culprit.
   The already resumed CLI keeps its loaded code; the fix takes effect on
   future CLI launches without rewriting any frozen product tools.
5. **Make README describes obsolete geometry limits.** Local commit
   `e56954b7` replaces the old 2–12 boxes/cylinders description with the native
   CAD toolkit's implemented capabilities, STEP-only exports, print evidence
   requirements, and physical-evidence limits. Local documentation links and
   whitespace checks pass. This is a documentation correction, not a new
   manufacturing capability.
6. **Status implies finalization during ordinary progress messages.** During
   the unfinished design handoff, status repeatedly reported activity
   `finalizing` while the CLI correctly said only that the Manager reported
   progress. Local commit `5bfd5c7c` names new message events `reporting` and
   presents old records with the same truthful public label. Legacy private
   hashes and checkpoint bindings remain valid. Turn completion and host gate
   acceptance are unchanged. Focused runtime, progress, CLI, and host tests
   passed 150 cases, with one existing skipped test. Clients that enumerate
   public progress activities must accept `reporting`.
7. **Round guidance omits concurrent STEP exports.** The second visual packet
   was invalidated by a separate assembly export during its render. Existing
   CAD guidance warned about concurrent warm commands, but did not explicitly
   cover cold `gen --write` calls. Local commit `a31cc595` clarifies in the
   Make-round skill and tool help that edits and standalone exports, including
   subagent work, must finish before a round and remain paused until its visual
   feedback is recorded. Read-only work may continue. Skill validation, help,
   self-check fixtures, and 36 existing tests passed. No verification logic,
   thresholds, or live frozen tools changed.
8. **Two native failures remain diagnostically opaque.** Local commit
   `472ac807` adds exact normalized classifications for four verified fixed
   messages in the installed native binary: closed in-process app-server,
   Luna output limit, invalid handshake state, and invalid cached login JSON.
   Multiple complete messages are 39 bytes, so the historical failures cannot
   be attributed from their length. Complete-message bounds, near-match
   rejection, private structured persistence, and unchanged retry eligibility
   are covered by tests. Validation passed 102 tests with one existing skip.
   The change adds diagnostic labels only; it neither retains raw error text
   nor authorizes recovery, changes the frozen runtime profile, or advances a
   gate.

9. **Selected mount obstacles lose ancestor transforms.** The live product's
   hardware-manifest builder identified this limitation and explicitly rejects
   nonidentity ancestors that would change its gate's obstacle poses. A
   separate repository reproduction confirmed the toolkit defect: selecting
   a nested leaf or group measured it in local coordinates while checking the
   whole assembly used its correct world pose. Local commit `339d9fe9` composes
   ancestor locations into non-mutating copies before indexing named nodes.
   A rotated nested-box regression now reports the correct 48 mm³ overlap;
   clearance failure, zero-clearance contact, duplicate labels, unnamed
   wrappers, and source immutability remain covered. Thirteen focused
   mount/motion assembly tests pass, as do skill validation and CLI help.
   Independent review found no issues. This is a source-tool fix only:
   collision thresholds and the live run's frozen tools remain unchanged.

10. **Bulk CAD generation loses deferred project imports.** The complete
    product export generated the apron entries, then failed while a guard
    generator imported `parts.apron_shell` and its `features.primitives`
    dependency. A fresh temporary two-entry project reproduced the same
    failure through the real cold `scripts/gen` CLI: source paths were restored
    after module loading, before `gen_step()` executed. Local commit
    `4953fc8e` keeps the existing source-derived paths active through both
    phases, restoring them on exit. Four new CLI/generator regressions cover
    deferred imports, failure cleanup, isolation and dependency tracking;
    five existing package tests also pass. Skill validation and independent
    review pass. No frozen product files were edited or refreshed. The native
    product independently moved its import before the later complete round.

11. **No explicit effort change on resume.** After the user changed the
    requested runtime effort to medium, local commit `fe2b9843` adds
    `resume --effort` for originally marked, profile-bound Codex token-budget
    runs. It records a validated private operator history, preserves the
    original immutable Manager/Wish bytes, and applies the selected effort at
    every later root native launch. Receipts distinguish effective effort from
    initial effort. Tests prove exact thread/runtime-hash continuity, unchanged
    accounting, same-command continuation, failure cleanup and rejection of
    unsupported or corrupt state. Thirty-five new and 308 existing tests pass;
    one existing test is skipped. Independent review found no blockers. This
    feature was applied to Cratercade through the normal CLI at 00:27:45 UTC;
    its next native launch still awaits the externally implemented 500M cap.
    Later observation found that restored child threads retained ultra even
    after the root changed to medium; that native propagation limit is being
    investigated separately and is not covered by the root-launch tests.

12. **The visual subprocess has an unadjustable 900-second cutoff.** The
    fourth full-product round reached that timeout without PNGs. Git attributes
    the shared timeout constant to `richter` in `98d263915` (2026-09-08); the
    visual step added by `reinSPQR` in `68a79b10` (2026-09-09) inherited it.
    Local commit `800ed9fd` adds `make_round --render-timeout SECONDS`, keeping
    900 seconds as the default and every visual check intact. It validates
    the limit before round work, records it, and preserves partial timeout
    diagnostics. A timed-out packet remains an error even if PNGs exist;
    rerounding can reuse unchanged passing print evidence but requires fresh
    hash-bound visual feedback. Fifty-six Make/registry and eight packaging
    tests pass, all six skill locks match, and independent review found no
    remaining issue. The lock update also catches up the two earlier committed
    CAD fixes, proven against exact tracked bytes. The timeout option is now
    materialized in Cratercade by the audited 00:30 host refresh; it has not
    yet been exercised by a resumed native turn. More tokens alone do not
    increase the visual command's chosen time allowance.

13. **Resume notification baselines and compaction spend differ from the
    durable native response ledger.** The 00:45 resume stopped at 00:46 on an
    ambiguous notification baseline. A metadata-only audit of all 17 native
    product threads found exact per-response, per-turn, and per-thread
    conservation at 102,371,792 tokens, including 2,050,831 compaction tokens
    missed by the notification reader and the interrupted resume's 178,760
    tokens. Local commit `1ca84eaf` binds and counts the exact response
    ledger, corroborates notification coverage, and binds compaction markers to
    already counted responses. The old strict reader remains for ledger-free
    streams. Resume must persist historical corrections before capturing the
    next turn's baseline; completed-turn recovery must retain the accounting
    method that produced its baseline. No private state has been edited to
    bypass the failed accounting check. Validation passed 425 tests, with one
    skipped; both independent reviews found no blockers. The live read-only
    parser returns the exact independently audited total. The current run's
    old accounting need is nonterminal and can recover through normal resume;
    ambiguous old completed-turn needs remain blocked by the accounting gate.

14. **Indexed CAD members retain expensive upward assembly links.** The live
    stationary audit's 73 completed conditions account for about 68.75 minutes
    of checking, mostly complete service sweeps. It builds once and already
    prunes disjoint bounds. A separate tiny synthetic reproduction confirms
    that `check_motion` and `check_mount` deep-copy parents and siblings when
    positioning a selected member. Motion indexing with 0/4/16 siblings made
    9/49/361 shape copies; moving one indexed leaf made 3/7/19 copies. The
    equivalent detached world-space leaf required one copy, with identical
    geometry and an unchanged source tree. Local commit `8ec75b9c` copies the
    selected subtree without following its parent link, preserving world pose,
    owned children and inherited colors without modifying source color caches.
    Motion indexing now makes 6/18/54 copies and mount indexing 3/11/35;
    subsequent leaf motion requires one. Nineteen focused geometry/copy/failure
    tests and broader motion, presentation, retention and packaging checks
    pass; independent review found no blockers. Only the CAD skill lock
    changed, and the audited 01:29 refresh installed the fix in this run.
    This proves a toolkit scaling defect, not its exact share of the live
    audit's elapsed time. The product's custom audit script and CAD geometry
    remain native-owned.

15. **A resumed root's medium setting does not retune existing children.**
    Live root metadata is medium while three restored child sessions remain
    ultra. Two isolated Codex 0.153.4 fake-provider trials reproduce this with
    exact synthetic root/child continuity. Setting the documented
    `agents.default_subagent_reasoning_effort` to medium also leaves the
    restored child at ultra. The native follow-up tool has no effort argument.
    No paid requests, production state changes, or global configuration edits
    were used. The prior host notice explicitly addressed Manager turns only.
    Local commit `275d1352` conveys the operator's superseding effort choice
    for subsequent native child work too, leaving all routing to Codex.
    Validation passed 179 focused effort, budget, accounting and host tests;
    independent reviews found no blockers. This is an intent handoff, not
    evidence that future child requests have changed or a claim of
    host-enforced child settings. The 01:59 normal resume applied the notice.

16. **Normal CLI interruption exposes a raw traceback.** The authorized
    01:58 interruption safely cleaned up but printed KeyboardInterrupt and
    internal stack paths. Local commit `49ff13a5` catches only KeyboardInterrupt
    at the outer CLI boundary after host unwinding, writes concise stderr
    guidance, and retains exit 130. Resume guidance quotes the supplied run
    id; Wish guidance does not assume persistence completed. Existing start
    loop behavior and other exception handling remain unchanged. Eighty-nine
    CLI tests pass, including cleanup order, JSON output, pre-persistence
    interruption and real subprocess exit behavior. Independent and root
    reviews found no blockers. No live run or runtime semantics changed.

17. **Make guidance does not explicitly review ordinary repeat use.** The
    pre-existing motion-review reference requires the complete mechanical
    operating cycle. The broader play/reload/reset/replay check was the
    observer's usability judgment after reading the current four-screw roof
    access instructions, not a pre-existing exact Make requirement. Local
    commit `9b249802` adds a short generic craft check to Make-round: compare
    repeated actions, tools, fasteners, hand effort, and disassembly against
    the Wish, and revisit that judgment in the existing final review. It adds
    no gate, artifact, review round, universal tool-free requirement, or
    physical Playtest claim. Thirty-four existing tests and independent
    reviews pass. As verified at 02:32 UTC, the running toy's frozen skill
    does not yet contain this addition; normal audited refresh is required.

18. **Render-package STEP provenance is captured before export.** The
    native Manager found the generated assembly sidecar naming a previous
    STEP hash despite its later timestamp. A synthetic real CLI reproduction
    confirms that one `gen --write` call captures provenance before its
    queued STEP export: the first write omits the hash and later writes can
    retain the old hash. Local commit `f7b10367` reads provenance inside the
    sequential package job after export. Alternate-export canonical paths,
    source bindings, geometry, and failure propagation are preserved.
    Forty-four tests pass, including byte changes with unchanged size/time,
    export/package failures, and real two-box CLI regeneration with STEP
    geometry checks. Root and independent review found no issue. This fix
    was materialized by the 02:55 audited host refresh. No product geometry
    or delivery files were manually changed by the observer.

19. **Review rendering also copies whole ancestors per leaf.** A separate
    synthetic reproduction found `render_review` making 3/35/323 shape copies
    for nested scenes with 1/5/17 leaves. Local commit `26dbe893` reuses the
    existing placed-subtree helper at the accumulated world transform, reducing
    those counts to 1/5/17. Exact geometry, labels, effective inherited colors,
    rendered pixels and source immutability remain covered, including copy
    failures. Ninety-one focused and integration tests pass; root and
    independent reviews found no issue. This fixes avoidable copy work but
    does not establish its share of the earlier timeout. It is separate from
    the surface-discarding `render_product` image defect. The source fix is
    committed and was installed by the 02:55 audited host refresh.

20. **Product presentation discards most large-model surfaces.** The failed
    hero passed 3,328,154 triangles to `render_product`, which kept only 75,000
    individual faces. That removed about 97.75% of the surfaces instead of
    simplifying them. A separate 259,204-triangle closed-cube fixture reproduces
    the holes and isolated panels. Local commit `7879ca9b` removes only the
    destructive sampling; all triangles remain available to the existing
    degenerate-face, visibility, framing, and state-difference logic.
    Thirty-one tests pass, with root and independent review. The fixed 500px
    synthetic render took 2.08 seconds versus 0.62 seconds before; production
    runtime and memory were not established by that small probe. The corrected
    renderer was materialized through the 02:55 audited host refresh.

21. **Clear source materials render opaque in required review evidence.**
    Native alpha probes produced identical images for opacity 0, 0.18 and 1;
    motion-state rendering also discarded alpha. Commit `aad0672f` retains
    effective source RGBA in both paths and composites transparent surfaces
    in exact per-pixel depth order, preserving the previous opaque renderer.
    Transparent shared edges contribute once, and strict depth/occurrence
    ordering prevents a near-coplanar peeling loop found during review.
    No geometry sampling or layer cap was introduced. All 107 focused CAD,
    exact GIF reconstruction, registry and packaging tests pass; six skill
    locks verify. Four opaque fixture views remain byte-identical. Matched
    before/after stills and an actual validated eight-frame GIF show the
    interior moving under the complete transparent cover. This is visual
    surface-opacity support, not physical optical or mechanical evidence.
    The fix was adopted by the audited 03:28 host refresh; its performance on
    the full toy remains to be observed.

22. **Ordinary native exec jobs survive the old SID-only shutdown.** A
    credential-free local-provider fixture using installed Codex 0.153.4 proved
    that an ordinary foreground exec command automatically gets a separate SID
    and survives the old guard even when that guard reports success. Commit
    `f9fc1d4e` requests native SIGINT grace, retains descendants by observed
    ancestry and creation identity, and scopes fallback signals to verified
    owners plus the original SID. Discovery uncertainty remains unsafe; failed
    cleanup cannot be hidden by a normal return, Ctrl-C, blocked pipe close or
    indefinite wait. A bounded watcher keeps discovery outside its state lock.
    There are 134 passing focused tests and one sandbox-dependent skip, with
    root and independent review. The final exact-source native fixture stopped
    the ordinary fresh-SID job, verified the original session empty, and left
    an unrelated same-workspace process untouched; no fixture process remains.
    Polling has an explicit observation race and is not universal containment
    or authority to recover work orphaned before this guard began observing.

23. **Verifier and finalizer require mutually exclusive review schemas.**
    ADR 0063 introduced schema 8 with exact `print_gate_sha256s` evidence in
    `stage_proposal.py`, but `verify_project` still rejects everything except
    schema 7 and omits the new field. The toy's bounded AST probe and root
    source inspection independently confirm that no one JSON file can pass
    both mandatory checks. Commit `4dfb4e68` aligns the verifier with schema 8
    while retaining passing report hashes and the explicit no-claim empty-map
    case. Canonical print reports embed their invocation arguments: prepare
    them with the verifier's exact canonical paths and options before review,
    then verify their bytes survive the final rerun. Copying round reports or
    swapping review versions cannot substitute for compatible gates.

24. **Real print reports omit the verdict their consumer requires.** The
    compatibility audit found that `check_thickness` and `check_overhang`
    compute and print a `RESULT:` line to stdout, but omit it from their
    Markdown report. The schema-8 finalizer requires the corresponding passing
    line inside each cited report. Existing synthetic fixtures inserted that
    line and therefore missed the incompatibility. Commit `4dfb4e68`
    writes the already-computed PASS or failure verdict to the report without
    changing measurement or exit behavior. Its integration test generates
    actual small-part reports, repeats the canonical verifier gate invocations,
    and feeds identical review bytes to both real validators. Existing toy
    reports need normal gate regeneration after the audited 03:49 refresh;
    the observer will not edit or retrofit their measured evidence.

25. **A generation-only size cap rejects large in-memory motion states.**
    `motion_presentation.py` rejects a canonical encoded state above 20 MiB
    after rendering its animation, while its validator reconstructs and hashes
    states without that limit. At 84 + 50 bytes per facet, generation admits
    at most 419,428 facets. The saved full-render log reports 3,328,154 facets
    at the same 0.08 tolerance for the current hash-matched assembly STEP,
    equivalent to about 159 MiB encoded; the exact canonical source-built
    state count has not been re-executed by the observer. Commit `4dfb4e68`
    replaces the additional full encoded-byte allocation with an exact
    streaming digest in generation and validation. Every triangle and pose
    remains bound; no geometry sampling is proposed. Existing file, GIF and
    state-count bounds remain. Synthetic large-state parity and reconstruction
    tests pass; the normal audited 03:49 refresh installed the correction.

26. **Opaque offscreen triangles can crash fixed-framing renders.** A wholly
    left-of-frame triangle produces an empty X-coordinate range, while NumPy
    interprets its negative depth-slice endpoint relative to image width.
    The real close-up failed with incompatible `(32,0)` and `(32,247)` arrays;
    a one-triangle fixture reproduces the same path. Commit `048f6ee5` adds
    only the two-line disjoint-bounds guard already used for transparency.
    Intersecting faces retain their existing pixels, shading and depth behavior.
    Forty-one new cases cover all sides/corners, opacity states, partial
    intersections, larger-render crop parity and unchanged input geometry.
    There are 118 passing renderer/motion tests and 24 passing registry/package
    tests; all six locks and independent/root reviews pass. CAD fingerprint is
    `6c830c3a0b8e3fea0f38714f59919a6718df52db277ff40ee3e482f6209907e0`.
    The audited 08:20 resume installed this source fix. The native product
    also retains its independently tested presentation-helper workaround.

27. **Mirrored CAD occurrences need explicit printable handedness.** The CAD
    reference required part builders and print entries but did not explain
    when a reflected occurrence can share a canonical entry. The actual door
    audit found one non-reusable receiver and five reusable parts. Commit
    `643da45b` clarifies that complete geometry must match by translation and
    proper rotation to share an entry; otherwise provide explicit handed
    builder variants, distinct print entries, existing print checks and
    matching handoff quantities/orientations. This is native craft guidance,
    with no new gate or schema. All 24 existing registry/package checks pass;
    root and independent review are clear. CAD fingerprint is
    `7727c075fc2d4c88083ed26cb4b8d6d95ed7b8cab1593dae4b1e633cc2dacf3d`.
    The active run made its own repair; the audited 08:20 resume subsequently
    installed this source guidance.

28. **Large Make-round output makes failed parts easy to overlook.** The
    round-seven summary contains three explicit thickness failures in both
    saved reports and JSON, while the native handoff initially mentions only
    the receivers. The JSON console representation is 62,605 characters;
    the rocker failure is approximately 35,744 characters into it. Human
    output already lists all three, so output size is a usability concern,
    not a demonstrated missing-verdict bug or proof of what caused the miss.
    Commit `abaf5a46` adds a complete failed build/print part index immediately
    after the human header and as the first JSON field. It derives from all
    recorded roles, including reused and older aggregate-only failures.
    Saved evidence writes, detailed fields, gates, warnings, caching and exit
    behavior are unchanged. All 74 focused and registry/package tests pass;
    root and independent review found no blocker. Only the Make-round lock
    changes, to
    `ecc66562ba138c122d4290ff5c5620b3806740e4521faed1e60ff0ab6ee30680`.
    The audited 08:20 resume installed this update. Earlier, at 05:42:34, the native
    Manager independently acknowledged and included the rocker repair,
    without observer feedback or a tool refresh.

29. **Native account-limit failures appear as unclassified.** The 06:07
    terminal event explicitly says the Codex account usage limit was reached,
    while Workshop retained an unclassified diagnostic. Commit `65f73e62`
    recognizes the bounded, anchored native sentence and reports category
    `usage-limit`, signature `usage-limit-exceeded`. Optional account-specific
    reset guidance is discarded. No limit, retry policy, event schema or
    saved historical diagnostic changes. All 104 native-session tests pass,
    with one skip; the new integration and rejection cases cover both terminal
    envelopes, privacy, sentence anchoring and the exact diagnostic bound.
    Projecting the observed 140-byte message now gives the expected diagnosis
    with both transport and service retry predicates false. No independent
    agent review was available because a helper hit the same account limit;
    root reviewed the diff and test results before committing.
    After the user restored capacity, an independent read-only review of the
    exact commit found no blockers in privacy, matching bounds, retry policy
    or test coverage. No further edits or tests were needed.

30. **Long motion checks expose no intermediate progress.** The full native
    check began at 08:52:41, with both its redirected result and stderr files
    remaining empty while it evaluates 1,303 conditions, including individual
    coupled sweeps with up to 9,600 steps. Empty output does not prove a hang.
    Commit `88d9f9a8` adds optional `--progress`: flushed, fixed-label phases
    and sample indices on stderr, globally throttled to five seconds apart
    except bounded setup/retention/final notices. Indices name the sample
    about to run, not completed work, percentages or an ETA. A long individual
    kernel operation can still be silent. Observer failures disable only
    diagnostics; default output, JSON/human result bytes, numerical order,
    thresholds, verdicts and exits remain unchanged. All 123 focused and
    existing motion tests pass, plus 24 registry/package checks and both
    self-check suites. Root and independent review are clear. The CAD-only
    lock becomes `0558555303e3f01646a5b4ebde9876e6089ad93e01626e0c705df1515f49b451`.
    This optional feature is deliberately not installed into the active run.

31. **Expensive endpoint pairs can still hide the moving mechanism.** Three
    exact camera trials failed to reveal the flag and bucket together through
    the enclosure. Commit `873f33ca` recommends small diagnostic previews of
    both endpoints and batched camera directions before final rendering.
    Geometry, materials, poses and tessellation stay unchanged; native Make
    selects the camera and shared framing, then completes the normal final
    evidence and reviews. All 24 registry/package checks pass and independent
    review is clear. CAD fingerprint is
    `ad46a0e2737358d6df5730ebea7de377a3cf970b3c64ef5c9f3cb2ebfc275e38`.
    This optional guidance is not installed into the active run, whose Manager
    independently started diagnostic previews before this source change.

32. **Late mount checks waste expensive downstream work after a repair.**
    The fresh mount gate found cup/nut contact after long motion, movie and
    delivery jobs had started; native Make correctly interrupted them before
    repair. Commit `046e11b6` adds craft guidance to refresh affected fit,
    mount and targeted clearance checks first, including nearby components
    reached by changed geometry even when their own source/manifest is
    unchanged. It adds no Python scheduler or per-edit full interference pass
    and preserves all final review and verification requirements. All 24
    registry/package checks pass; independent review is clear. CAD fingerprint
    is `e3aa6166c7c0b714ad9bb760872548aaf060cf05d85d117447460672f89e43e8`.
    This optional guidance is not installed into the active product run.

33. **The host terminated Codex on nonterminal native reconnect notices.**
    Credential-free Codex 0.153.4 fixtures proved truncated SSE and SSE idle
    timeout reconnects can precede successful completion in one native process
    and thread. Commit `41fd7b2f` recognizes only those two exact bounded
    message forms with valid counters and the exact event shape. Other errors,
    extra fields and terminal `turn.failed` retain their failure behavior.
    No host retry loop is added. Identity, completion, accounting, finalization
    and process cleanup requirements remain intact. All 111 runtime tests pass
    with one skipped, plus an actual-native host stream fixture; independent
    review is clear. The 100- and 89-byte fixture messages do not establish the
    cause of the historical unclassified 39-byte failures. This host-only fix
    loads on normal resume and changes no frozen product tool or gate.

34. **Imported-solid repairs need proof across actual source placements.**
    Commit `d77a0301` adds generic craft guidance for solids that validate
    locally but fail after placement. It requires actual transform/mirror
    chains, preserved supplier bytes and validators, Boolean/bounds/topology
    comparisons, STEP round trips and converged mass-property checks when
    default volume changes. Failed Booleans cannot count as empty differences,
    and sampled surfaces cannot prove global equality. It authorizes no
    automatic conversion, tolerance relaxation or physical claim. The 24
    existing skill-registry/package tests pass, all six locks verify, and root
    review is clear. CAD fingerprint is
    `abc49312ae9fb8c87ed794c94dba277fa02edc80c9578bc910d5a6b019b7cc0c`.
    This optional source guidance is not installed into the active toy.

The candidate motion-file size audit found a 49,000,077-byte indented JSON
document and a lossless compact serialization of 9,124,598 bytes. The frozen
motion-presentation reader has a 20 MiB input bound, so compact serialization
fits while retaining all 1,303 conditions and their data. No product file was
rewritten, reader bound raised, or gate pass inferred. Final serialization and
hash binding remain native Make work on resume.

Read-only refresh preparation at source `abaf5a46` verified all 243 frozen
inputs (2,842,907 bytes), including hashes, sizes, modes and tree inventories,
against stable checkpoint revision 10. Of 165 carried refresh-eligible files,
exactly four differ from source: CAD `scripts/render_review` and
`references/project-structure.md`, plus Make-round `scripts/make_round` and
`SKILL.md`. All five lifecycle allowlist files match; all six source skill
locks verify. No adds, removals, mode differences or unexpected drift were
found. Nothing was refreshed. Repeat the audit at an actual terminal
checkpoint before any normal resume that adopts these updates.

After the actual 06:07 terminal failure, root repeated the read-only audit at
source `65f73e62`: all 243 frozen inputs and their modes/inventories still
verify, the checkpoint stayed stable, and the same four of 165 eligible files
are the only source updates. All five lifecycle allowlist files still match.
At that audit nothing had been refreshed or resumed. The runtime diagnostic fix itself is
host code and does not change frozen product-tool bytes or runtime policy.
The subsequent normal 08:20 resume installed those four audited skill changes
and successfully continued the same native session. The optional 09:25
motion-progress source update remains outside the frozen run.

## September 11, 17:23 UTC observation

The original CLI resume remains live (session 92434), on medium effort with
399,541,626 of 500,000,000 native tokens observed. Publication is not created.
The full r6 motion report is still empty. A read-only process inspection at
17:22 found its checker running for 3:08:43, using 100% of one CPU core with
188:18.81 accumulated CPU time. Earlier short stack samples showed OCP Boolean
and surface/volume work. This establishes computational activity, not a
completion percentage or passing result.

The r6 animation completed around 16:58: 48 declared states encoded as 45 GIF
frames with holds. Its GIF hash and all 148 recorded source bindings were
verified against the saved files. Independent review round 1 failed the
presentation: the roof/frame obscures the bucket-to-flag coupling and raised
flag, and the overview does not communicate the central play mechanism clearly.
These are visibility findings, not evidence of physical operation or failure.

Make saved a higher hero camera, then completed the bounded close-up comparison
in `design/signature-camera-repair-r10/HANDOFF.md`. Four directions at both
endpoints retained all 542 occurrences; none exposed the full bucket, common
arm and flag at both endpoints. This does not prove every possible camera
would fail. The Manager announced a focused geometry repair preserving
containment and access. That repair and its affected engineering checks remain
pending. The old animation and engineering passes cannot establish acceptance
of subsequently changed geometry. No canonical passing reviews, final verifier,
Made outcome or authenticated publication exist at this observation.

## September 11, 17:37 UTC repair handoff

The native Manager accepted the completed
`design/jackpot-visibility-repair-r11/HANDOFF.md` for CAD construction. Root
read the design and verified all four handoff artifact hashes. The proposal
raises the complete roof/access module 24 mm, extends rooted posts/mullions
and side/end PET, and preserves the jackpot and marble route. Seven printed
types change. A common assembly translation preserves local door geometry;
changing individual absolute hinge heights would misalign it.

The sampled conservative ray-envelope calculation gives about 2.39 mm of
roof clearance for the raised pole at a 125-degree azimuth, 10-degree elevation
camera. It is a reason to test this repair, not a rendered visibility pass or
continuous exact-geometry proof. Taller-support stiffness and physical tip
stability remain untested. The handoff requires updated print, mount, motion,
containment, six load/reset paths, balance, delivery and visual evidence. The
baseline motion job is still live without a verdict and must remain bound to
its original geometry. No repaired CAD acceptance or publication is established.

At 17:38, native Make started construction and stopped its baseline motion
checker before the geometry change. Root inspected
`design/check-motion-r6-interrupted-before-visibility-repair.json`: exit 130,
verdict null, original motion hash and 148 source hashes preserved. The reason
is the accepted roof repair, not a proved checker failure. The long baseline
job produced no passing verdict and must not be counted as completed motion
verification. Host CLI session 92434 remains live; the motion subprocess
termination is not a stopped Workshop run.

## September 11, 17:56 UTC construction and print evidence

The completed r11 implementation audit accounts for all 212 enclosure
occurrences: 170 translated upward, 20 extended and 22 unchanged. Its failure
list is empty. Root verified all 148 current source hashes and six evidence
artifact hashes against the handoff at 17:46. The bounded audit preserves root
interfaces and door-relative geometry; it is not full assembly acceptance.

Root inspected `visibility-r11-all-targets-gen.log`: 79 successful generation
records, covering 78 printable entries and the combined assembly. The saved
STEP comparison reports exactly seven changed printable files and 71 identical
files. Actual end-join depth is 30.7 mm, correcting the design estimate of
30 mm without narrowing the part.

Round r0013 has written seven changed-part thickness reports and seven
overhang reports. Root read their results: all seven say printable at the
declared wall and all seven say prints unsupported, with no failed rows.
These individual reports do not establish completed round acceptance. The
round summary and visual packet, current mount inventory, revised handling
and full motion, independent review, final verification and publication remain
pending. Native Make owns those checks in the still-live CLI session 92434.

## September 11, 18:13 UTC revised visual and static evidence

Round r0013 passed numerical checks and native visual feedback. Root verified
all 230 bound source/export files, three image hashes and feedback packet hash
`deccab2c1786674a79a15c6972e6ca7adc40e7453f1769a95200c35253ced32c`.
This round does not include full motion or final integrated verification.

The r10 inventory contains 542 leaves and 398 mount records; its manifest hash
is `fdf11f802bea3fa0b046e0a2896cb5dc5b88cfb6faa46d66b4e6fd9ed231cbf1`.
The native mount report passes all 398 measurements with no findings. Mission
A/B static overlap reports pass with 1816/1820 candidate pairs and no clashes;
all three process exit codes are zero. These are overlap/mount checks only.

Root inspected both `visibility-presentation-r11/state-*.png` endpoint images
and verified eight input and three output hashes. The raised flag is now
visible below the roof; the tipped bucket contains the visible marble and
the connecting arm is substantially exposed. A short arm segment remains
behind fixed support hardware. Native Make accepts the view for signature
evidence, subject to fresh independent review and animation; stills establish
neither continuous motion nor physical operation.

Of twelve r10 mount/static artifact hashes, eleven match current bytes. The
aggregate `native-mount-static-r10-run.log` hash matches its first six lines
exactly, before 1632 bytes of final summary output were appended. The JSON
result hashes match, but the completed aggregate log is not fully bound by
that recorded hash. Do not claim every artifact binding matches. Handling,
full motion, final review/verification and authenticated publication remain
incomplete in the live CLI run.

## September 11, 18:27 UTC handling and validity

All six revised handling paths are complete and pass. Root verified their
six report hashes, exit codes zero and stable-source declarations: loading
15.5/16/16.5 mm uses 2575/2573/2572 samples, and reset uses 1826/1827/1828.
These are sampled geometry checks, not physical usability tests.

The r10 native validity/balance batch passes with all artifact hashes matching.
Validity covers 542 occurrences with zero failures; the calculated balance
audit passes seven checks under its assumed densities and friction exclusion.
Complete revised motion remains pending; native interference and delivery
generation are separate ongoing work.

Native Make separately reconciled the earlier aggregate-log hash mismatch in
`native-mount-static-r10-log-hash-clarification.json`. Root verified the preserved
original summary and completed-log hashes. Canonical hero and signature image
hashes also match `canonical-presentation-r11-binding.json`. New animation and
independent review remain pending; the old movie does not qualify raised-roof
geometry. No publication exists yet in the live CLI run.

## September 11, 18:50 UTC delivery and review progress

Root verified the completed revised delivery package: 542 unique STEP paths,
exact matching file set, all part hashes matching the delivery report and
generated originals, no current source-binding mismatches, and matching report
and assembly-index hashes. Combined STEP is
`c1a3531835d8104465d1b80048bfc4e35501b9594e7d88ed863ef226ba87777e`;
package index is
`3a63eec65114f8341d97eae3a8ba586b7233577b7b40c673afba419af85210db`.
This is a completed file package, not publication or physical manufacture.

The r7 full motion and animation jobs restarted around 18:30 after launch-only
path, manifest-size and resolution issues. Root verified parsed equality of
the 49,226,659-byte precompact manifest and 9,190,093-byte current manifest,
including all 1,303 conditions. Current manifest SHA256 is
`a2b51c8cca91cec7020643344a3163116fd96d7a4047a29f95eb71695f6eaaf1`.
The animation plan uses 48 exact states, rear camera 125/10 and supported
1024-pixel output. Neither job has returned a result; the existing canonical
movie is still the rejected pre-repair version.

Root read review-two `fresh-stills.json`: no remaining still-image blockers,
prior Wish knowledge explicitly disclosed, no full-review pass. Remaining
questions concern readable independent controls, ordered tipping/reset and
ball handling. Four exact-source control states exported; supplemental views
are being camera-corrected for occlusion without changing product geometry.
Native CLI 92434 remains live, medium effort, 444,116,464/500,000,000 tokens
observed. Full review, final verification, Made and authenticated publication
remain missing.

## Observation rules for the rest of the run

Poll the live CLI process and inspect the saved run's status and artifacts.
Quiet output is not evidence of a stopped process. Preserve this run/session
when recovering from a supported failure. Read exact product images and
engineering evidence before assessing the toy's result.

Fix reproducible Workshop/Make bugs in their owning source components with
contract and failure-path tests. Do not repair the toy by manually changing
its sealed artifacts, weakening a gate, or substituting a different toy.
Respect frozen tool bytes when planning recovery. Any physical rolling,
elastic performance, durability, or player-enjoyment claim still requires
appropriate physical evidence.

## September 11, 19:15 UTC control evidence and live computation

Root viewed the completed four-state overhead control sheet: neutral, left-only,
right-only and launcher pull. Separate blade/thumb-pad changes and the extended
orange launcher shaft are visible. The apron and roof still obscure parts of the
connections; these static views do not prove return force, launch, or dynamics.
Root rechecked its SHA256 against review-two fresh-controls.json:
`61674321707050a21b43cf5d1153800e63021cdf25e5b0c117baef8d2b060cf4`.
The critic resolves control recognition but explicitly leaves the full round
pending the fresh actual movie. Current r10 interference report also matches
its completed engineering summary's recorded hash: zero clashes, no truncated
pairs at the declared 1 mm3 overlap tolerance.

At 19:13 UTC the two current jobs had run about 43 minutes. Read-only process
inspection identified PID 56274 as check_motion and PID 56275 as
motion_presentation.py; CPU time advanced across two observations, at about
99% and 129% CPU respectively. CLI session 92434 is confirmed live. Neither
has returned a final result. Older interrupted motion runs are not passing
results. Root's private observer now labels the current r10 engineering summary
instead of displaying superseded r8 assembly and isolated candidate checks.

## September 11, 22:40 UTC animation and review checkpoint

The r7 animation completed at approximately 21:12 UTC. The canonical movie is
1,823,909 bytes, SHA256
`cf8f1ca3380da02d83eba9cfef8f9ad07f9764fc3d895aae1df9dbe0d17e8bfe`.
Its evidence file SHA256 is
`5f58ee6b7cb365dd2da2703004a4105761f2e152d45926c30828f22d55aa016d`.
Root rechecked both exact files at this checkpoint.

Canonical independent visual review round two passes, including
`motion_matches_wish: true` and all signature recognition booleans. The review
explicitly discloses prior Wish knowledge: this is an informed rereview, not
a fresh blind read. Visual acceptance is not a final Make gate or proof of
physical play, fit, rolling, elastic forces, or durability.

CLI session 92434 remains live. At approximately 22:34 UTC, read-only process
inspection showed motion-check PID 56274 at 4:03:45 elapsed, 244:05.60 CPU time,
and 99.4% CPU. The latest observer still has no full-motion verdict, integrated
verification report, Made artifact, or authenticated publication. Quiet motion
output must not be reported as a pass or treated as a terminal process.

## September 12, 01:49 UTC token-budget shutdown

CLI session 92434 exited with code 2 at 01:49:14 UTC: `product token budget
stopped native execution`. Observed usage is 500,096,932 against the authorized
500,000,000 tokens. This handle is terminal; do not keep describing it as live
or launch a duplicate resume. A higher allowance requires operator approval.

The r7 motion check ended without a verdict: `design/check-motion-r7.json`
remains zero bytes. Read-only process inspection after the shutdown found no
PID 56274. Its last CPU observation at approximately 01:45 UTC showed 7:15:42
elapsed and 436:02.56 CPU time at 100% CPU. The check ran roughly 7 hours
19 minutes before shutdown; neither CPU activity nor elapsed time proves a
passing motion result.

The canonical animation, evidence, and visual reviews still exist at their
previous recorded sizes. The integrated verification report, Made artifact,
and authenticated publication are still absent. Resume must preserve the same
native session and frozen product tools and complete those remaining gates;
the completed visual review is not a substitute for mechanical validation.

## 2026-09-12 07:19 UTC — exact runtime-device recovery

The 04:15 continuation stopped at 06:13:44 with `unsafe-process-reap` and
`process_tree_reaped: false`; host handle 35621 exited 2. Used tokens were
505,474,493 of 1,000,000,000. Final verification did not record a pass and Made
and publication remain absent. The two surviving motion processes inspected
with `lsof` belonged to a different Wish, not Cratercade; they were untouched.

Ordinary resume 72865 and diagnostic resume 6054 both exited 2 before native
launch with a checkpoint-binding error. Read-only comparison proved all static
checkpoint fields match; only runtime fingerprint differs. Reconstructing the
previous filesystem device number 16777231 in place of current 16777230,
leaving every path, inode, mode and other runtime policy field unchanged,
reproduces the saved fingerprint exactly:
`8f1fbd99effe118fa1fc9f3015f41efdc9cd7148d6e8a2f5496adcf90e57d37b`.
Current fingerprint is
`68a911af804b7502e09027d42670d0c0d38b764d85fc98449c7d586cfdcefeea`.

Builder commit `53f7fef4` adds explicit host-only
`WORKSHOP_CODEX_RUNTIME_DEVICE_RECOVERY=OLD:CURRENT`. It accepts only the exact
reconstructed fingerprint, preserves the original checkpoint, launches using
current identities, and records a private hash-bound recovery receipt. It
never forwards the recovery variable to the native agent. Tests cover default
refusal, explicit recovery and repeat resume, unrelated inode/mode/path/model/
CLI drift refusal, malformed mappings, and credential-environment isolation.
Native-session/process-ownership suite: 128 tests, one skipped, otherwise all
pass via unittest; diff whitespace check passes. No frozen product tool was
refreshed and no product gate was waived. No source push was performed.

The normal CLI resumed at 07:19:11 through live handle 32749, with fresh native
reasoning confirmed. Same native thread `01a08b82-68d1-72c0-8876-a9a9074885d8`,
Astra medium and 1B token cap. Exact command:

```sh
WORKSHOP_CODEX_RUNTIME_DEVICE_RECOVERY=16777231:16777230 WORKSHOP_CODEX_BIN='<operator-home>/.local/lib/node_modules/@openai/codex/node_modules/@openai/codex-darwin-arm64/vendor/aarch64-apple-darwin/bin/codex' .venv/bin/python -m cli resume wish-20260910-132933-a88b698e --max-tokens 1000000000 --effort medium --strict
```

Recovery receipt SHA:
`ccfa22ae44ac74015946df554ec51f0e966c5fff9eb68e7215cfa59690c3de2f`.
This proves session recovery, not final mechanical verification or publication.

At 07:21 the restarted integrated verifier r5 is confirmed as PID 10879,
parent 10404, using approximately 120% CPU, within live CLI 32749. Its native
session is 85277 and log is design/verification-pipeline-r5.log. Current frozen
command uses `verify_project . --strict-fit --print-gates --nozzle 0.4
--overhang-angle 45.0 --report measure/verification-pipeline.md`, with Python
`-u`. No final result is recorded yet. Root separately compared normalized
canonical reviews to the original independent round-two files: signature JSON
parses identically; motion JSON differs only in `blind_motion_read`, exactly
by removing the recorded redundant Frame8 sentence. Both current review hashes
match review2-canonical-final-binding.json. Verdicts, reviewer identity and
review-round count remain unchanged. CLI 32749 was polled live again after a
50-second bounded wait; no terminal result or publication was reported.

## 2026-09-12 07:24 UTC — final verifier's silent animation replay

Read-only inspection of the frozen verifier explains why r4/r5 logs remain
empty despite CPU work. Before constructing its logged Runner, final-mode
`verify_project` calls `motion_presentation.validate`. That function first
checks cheap hashes/review, then reconstructs all selected motion states,
checks each geometry digest, and invokes `animation_bytes` again to compare
the regenerated GIF hash with the submitted GIF. This is deterministic
anti-substitution verification, not a new creative animation or review.
The original r7 GIF took roughly 2h42 to generate, so a long silent replay is
plausible before the mechanical pipeline even starts. Do not describe the
empty r5 log as proof that the full mechanical motion sweep has begun.
At 07:23:49 PID10879 was live, elapsed3:47 CPU4:55.97 at99.6%; it is untouched.
Future workflow observability improvement: emit explicit replay-start/end and
state/frame progress before expensive preflight work. No frozen checks have
been weakened, no GPU change made, and no in-flight refresh performed.


### Final r5 advances beyond animation replay

Observed 2026-09-12T10:07:31.322784+00:00: the same live CLI 32749 / verifier PID 10879 has advanced beyond frozen motion-presentation preflight into the logged engineering pipeline. Layout rc=0 (0.21s), generation rc=0 (76.52s), strict fit rc=0 (42.02s; 78 printable targets on a 220mm bed), custom fit rc=0 (2.51s; 151 checks, no failures). check_mount launched; no final verdict. Canonical verification report still contains preserved earlier formatting refusals and must not be read as the r5 result. No new process, frozen-tool refresh, or gate waiver was performed.


### Final r5 mount PASS; full motion active

Observed 2026-09-12T10:11:16.627456+00:00: verification-pipeline-r5.log reports check_mount rc=0 in 201.38s, all 398 declared mounts seat their components. The same verifier PID 10879 now owns child PID 86648 running frozen check_motion . --manifest measure/motion.json; child elapsed 1m23s, CPU 2m22.51s, 98.9% at observation. Full motion has now started inside the integrated pipeline; its verdict remains pending. CLI 32749 confirmed live. No physical performance claim follows from mount seating.


### Operator pause — 2026-09-12 22:34:52 UTC

User requested stopping the CLI to fix motion verification and resume later.
Sent SIGINT only to confirmed host CLI PID 10380. CLI session 32749 exited
130 with “workshop: interrupted” and its saved-run resume instruction.
Confirmed PIDs 10380 (host), 10404 (native Manager), 10879 (integrated
verifier), and 86648 (motion checker) are all gone. No automatic resume.
Last motion observation: elapsed 12h25m05s, no verdict. This is an operator
interruption, not a passing or failing motion result. Existing renders,
animation, review evidence, and saved native session remain preserved.
Observed usage at stop: 608,046,967 / 1,000,000,000 tokens. Make remains
unfinished; publication is not-created. Native session:
01a08b82-68d1-72c0-8876-a9a9074885d8.

Last successful resume command (record only; do not run until requested):

```sh
cd <operator-home>/code/autonomous-workshop
WORKSHOP_CODEX_RUNTIME_DEVICE_RECOVERY=16777231:16777230 WORKSHOP_CODEX_BIN='<operator-home>/.local/lib/node_modules/@openai/codex/node_modules/@openai/codex-darwin-arm64/vendor/aarch64-apple-darwin/bin/codex' .venv/bin/python -m cli resume wish-20260910-132933-a88b698e --max-tokens 1000000000 --effort medium --strict
```

The saved run uses frozen CAD tools. A source-only motion fix does not by
itself change those tool bytes; resolve the supported migration/recovery
path before resuming. Do not weaken verification or treat an interrupted
run as completion.


### Motion fix and attempted resume — 2026-09-14

User explicitly selected resuming Cratercade after checking the motion fix.
Ported exact placed-material cache from ef0b2bc7 into c4e7549c; canonical
CAD skill fingerprint is 97132b4f4f20775f5f03993a093e61c03896cc81e60f4f547d17dad5a44492ce.
Validation: 124 pytest tests and 160 subtests passed, including cache identity,
placement/orientation, failure behavior, unchanged sampled collision results,
validity, Boolean measurements, drive evidence, progress and skill lock.
Temporary pytest dependencies live in /tmp/cratercade-pytest, outside .venv.

Authorized CLI resume used the prior recorded command plus --refresh-tools.
Host reported four rebound paths: cad/scripts/check_motion and references
inspection-and-validation.md, motion-manifests.md, run-cost.md. CLI handle
10592 exited 2 before native session launch: token-budget rollout adapter
requires validated Codex 0.153.4. The configured binary now reports 0.154.0.
No live Manager or motion verifier was launched by this attempt. Do not claim
resumed computation or benchmark speedup for the full product. The source fix
and audited tool refresh succeeded; runtime compatibility remains unresolved.
Do not silently relax accounting or runtime identity checks, replace global
Codex, or start a new native session to bypass this refusal.


### Codex 0.154.0 resume-accounting probe

A credential-free loopback probe used the installed real 0.154.0 binary,
a disposable native home/workspace and two controlled Responses completions.
No product session or credentials were used. Report:
/private/var/folders/4c/lq3h60sn5l53hs3s21nzpqzw0000gn/T/cratercade-154-probe-j3bzji_u/report.json
Native fixture thread: 01a0a030-0e6d-7282-ae18-8b4ea0a8a681.
First request: 100 input / 10 output. Second request: 200 input / 20 output.
The unchanged file parser (version override isolated to probe memory) returned
100/10 then 300/30 correctly. However, native turn.completed on resume also
returned cumulative 300/30, rather than second-request 200/20.
Workshop's current completed-turn reconciliation compares current >= baseline
+ terminal, which would require 400/40 here and reject valid 300/30.
Therefore merely accepting 0.154.0 would be incorrect. Need either validated
version-specific terminal reconciliation plus an audited runtime migration,
or a supported isolated 0.153.4 runtime relocation. No production version
allowlist, session binding, or accounting gate changed during this probe.


### Team sync and successful native resume — 2026-09-14 14:03 UTC

Pulled origin/main through 52fef891; merge 350efc33 uses the team's exact tree
plus the previously authorized 1B-token maximum. Prior local work remains in
backup/cratercade-before-team-sync-20260914; unfinished 0.154 experimentation
remains stashed and was not applied over team code. Team policy defaults motion
verification/animation review off on resume; this run explicitly selected
--check-motion false, so motion is unverified, never passed.
Restored saved explicit effort support in 7950a095 (274 tests, one skipped).
Restored historical response-ledger accounting alongside team replay and visual
records in e72d7219 (207 tests, 263 subtests). Offline accounting reproduced
608,046,967 tokens across all 29 product threads before launch.

CLI handle 74379 is live. At 14:03:09 session.resume started and native Manager
reported reasoning/progress. Do not launch a duplicate. Command:

```sh
WORKSHOP_CODEX_BIN='<operator-home>/.local/lib/node_modules/@openai/codex/node_modules/@openai/codex-darwin-arm64/vendor/aarch64-apple-darwin/bin/codex' .venv/bin/python -m cli resume wish-20260910-132933-a88b698e --max-tokens 1000000000 --effort medium --check-motion false --strict
```

Earlier handle 98441 exited 2 on historical usage ambiguity; the corrected
handle above starts the same native session under team CLI-upgrade handling.
Publication and Make completion remain unproven. Tools had already been
refreshed through the host before this invocation; no manual toy edits.


### Resumed verifier passed — 2026-09-14 14:38 UTC

The same live CLI session (74379) completed `verification-pipeline-r6.log`
with `final workflow: rc=0 in 2063.66s`. All 78 printable part sources
passed each of check_mesh, check_overhang and check_thickness (234 passes).
The canonical `product/cad/measure/verification-pipeline.md` is 77,321 bytes,
SHA-256 `99ad690d2364bf6b0bc22623a217b6ff51f3dee0a6267bb2ae67321e5332acc2`.
Motion is explicitly NOT RUN under the current Make policy and remains
unverified. No physical test is implied.

The native Manager's first subsequent Make finalizer attempt found changed
print-report hashes relative to the earlier review. The Manager is reconciling
the exact current reports and coordinating a native subagent. Made and
publication were not yet complete at this observation. Usage observed at
14:39 UTC: 612,905,105 / 1,000,000,000 tokens, medium effort, gpt-6-astra.


### Resume compatibility repairs — 2026-09-14 14:54 UTC

CLI 74379 terminated at 14:43:47 UTC after the native Manager produced a
waiting outcome. Print measurements had passed, but the refreshed Markdown
writers omitted the RESULT summary required by the frozen finalizer. The
terminal host reconciliation also compared a restored notification baseline
against lifetime response-ledger totals.

Commit a1fb2338 restores truthful PASS/FAIL report summaries and accepts an
exact ledger-covered terminal notification only with advanced lifetime usage.
Offline replay proved 615,747,773 tokens across 30 threads. Regression suite:
286 tests and 394 subtests passed. A PDF test initially inherited the temporary
pytest PYTHONPATH; it passed when pytest was loaded through sys.path instead.

CLI 47856 refreshed exactly check_overhang and check_thickness, reconciled
usage, then refused the old waiting outcome's pre-refresh checkpoint.
Commit 75200656 preserves exact predecessor Make outcomes in private quarantine
across explicitly recorded tool-refresh corrections, requiring fresh native
finalization. Unrelated stages/checkpoints remain refused. Tests: 58 passed
and 6 subtests passed.

CLI session 5808 successfully resumed the exact native root at
2026-09-14T14:53:41.925Z with medium / Astra / 1,000,000,000 tokens and
--check-motion false. No new root session and no publication yet.

```sh
WORKSHOP_CODEX_BIN='<operator-home>/.local/lib/node_modules/@openai/codex/node_modules/@openai/codex-darwin-arm64/vendor/aarch64-apple-darwin/bin/codex' .venv/bin/python -m cli resume wish-20260910-132933-a88b698e --max-tokens 1000000000 --effort medium --check-motion false --strict
```


### Make accepted; host-only Release resume — 2026-09-14

All 156 r7 report regenerations passed and reproduced the original independent
review's report hashes. The native Manager restored that exact review,
preserved excluded generated data outside the product, and finalized Made.
CLI 5808 completed its native turn at 15:07:12 UTC, reconciled usage, and the
host accepted Make at 15:07:42 UTC (`gates/0015-make.json`). Lifetime usage:
619,141,459 / 1,000,000,000 tokens. Made is 541,761 bytes.

Release preparation then refused because its 512 KiB native packet duplicated
the entire large Made manifest. Commit a0705728 omits only that duplicate for
host-only Spark schema-4 Release; exact path/file/contract/manifest bindings and
the complete host-side validated contract remain. Large real manifest fixtures
prove synthetic publication without a native Release turn; changed sealed
contract bytes still fail. Regression suite: 115 tests / 116 subtests passed
under sandbox; the sole loopback socket test then passed with local networking
(1 test / 2 subtests). The narrowed tampering test also passed separately.

CLI 92328 resumed the authorized publication with the same command/settings.
Its host PID 53222 was confirmed live at 98.8% CPU after 1m24s. No publication
receipt yet at this observation; do not treat resume as completion.


### Published as Dee — 2026-09-14 15:26 UTC

CLI 81848 exited 0, complete at Release, after compressed Factory import and
publication succeeded. All completion assertions passed; see
[completion record](cratercade-completion.md). Final usage 619,141,459 / 1B.
