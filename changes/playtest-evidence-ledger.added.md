- Bank every sealed Playtest's feedback in the cross-run evidence ledger
  (`$WORKSHOP_HOME/evidence/evidence.jsonl`), hand later runs sharing a
  mechanism their strongest ten rows as `prior_evidence`, write confirmed
  vault leads back to the host vault's anti-pattern nodes, queue dismissals
  under `vault/_review/`, and add `workshop evidence harvest|list|recall` and
  `workshop vault review`.
