Freeze a portable native stage-objective interface for new product runs.
Codex retains its Goal API; other Managers use their native session/task
interface. Resolve Inventor roster paths from MANAGER.json and explicitly
retain mandatory independent reviews during autonomous recovery. File runners
use strict terminal status so a waiting run does not appear complete to a
shell caller; Claude defaults to native Sonnet and progress labels are neutral.
