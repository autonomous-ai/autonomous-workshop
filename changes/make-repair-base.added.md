- Redirect a Make repair to the best sealed round: Playtest receipts record
  failing-check and actionable-feedback counts, and when the last round is
  strictly worse than an earlier one the next Make packet names that round's
  hash-verified `repair_base` instead of the last revision.
