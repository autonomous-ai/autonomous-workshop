Add `references/cad-tool-contracts-v1.md`, the complete `gen`, `export`,
`render_product` and `verify_project` interface Make needs, and link it from
the Make contract. `verify_project --help` prints only the first line of its
module docstring, so runs were reading disjoint ranges out of its 2,288-line
source to rediscover a mode, a report path, or an ordering rule. A test binds
every documented flag, report path, refusal and default to the vendored CAD
skill so the reference fails loudly on a resync rather than drifting.
