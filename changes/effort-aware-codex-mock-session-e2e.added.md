- Add an opt-in authenticated Codex context-and-integration acceptance tier for
  the current selectable lifecycle: Spark runs `Make -> Release`, Forge runs
  `Invent -> Make -> Release`, and Quest runs
  `Invent -> Make -> Playtest -> Release`. One real persistent Codex session is
  started and resumed through terminal published Release; there are no Match or
  Concept turns. Test-only final-byte/run-root context proofs, a loopback
  Factory transport fixture, bounded local tooling, and an operator-run route
  matrix keep ordinary CI deterministic and credential-free.
