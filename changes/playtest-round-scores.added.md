- Score every Playtest round as Codex-authored evidence: the `agent-playtest`
  check carries at least three blind reads on `wish_fit`, `play`,
  `legibility`, and `build_confidence`; the finalizer and host compute the
  median and spread, refuse a `pass` below the floor, record them in the gate
  receipt, hand the next Make its `score_history`, `regression`, and
  `ambiguous` dimensions, and `workshop status` prints one line per round.
