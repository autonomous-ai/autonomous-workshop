- Derive Factory's narrow multipart occurrence sidecar from a sealed native
  Make `product.json` inventory when the adjacent STEP JSON is a richer CAD
  descriptor, preserving exact hashes and shell-count checks without editing
  the product artifact.
- Publish Release's sealed customer manual when native Make also carries an
  engineering manual at the same root path.
- Add a temporary, deterministic `project.json` required by Factory discovery,
  and preserve root `assembled.stl` so Factory selects the full toy for its viewer.
