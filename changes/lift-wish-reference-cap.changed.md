- Raise `MAX_WISH_REFERENCES` from 8 to 99 so a complete board-game set can seal
  one reference image per unique geometry inside the Wish boundary. The
  `ref-NN-` filename pattern is the hard ceiling; the total reference byte
  budget is unchanged.
