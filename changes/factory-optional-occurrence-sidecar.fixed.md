- Ignore product-specific or malformed optional STEP sidecars during Factory
  import and fall back to the sealed root `assembled.stl`; preserve multipart
  component transport only when its native descriptor validates completely.
