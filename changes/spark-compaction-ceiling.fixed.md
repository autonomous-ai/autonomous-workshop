Raise the frozen Spark context-compaction ceiling from 64,000 to 256,000
tokens for new runs. A complex Make compacted every few requests at the old
ceiling, dropping the stage packet, tool contracts and passed-check record it
had just built, then re-reading the same bytes and repeating the same failed
repair. Runs frozen at Spark v1, v2 or v3 keep their historical ceiling on
resume, and every other frozen setting, work order and gate is unchanged.
