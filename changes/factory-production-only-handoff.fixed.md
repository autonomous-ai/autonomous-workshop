- Limit Factory mesh handoffs to the root viewer assembly, required occurrence
  metadata, and declared production STLs so alternate CAD and slicer formats do
  not inflate the provider's fallback part-count price estimate.
