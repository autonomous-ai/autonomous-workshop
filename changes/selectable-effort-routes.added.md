Add selectable Spark, Forge, and Quest routes to `workshop wish` (selected
with `--workflow`; originally `--effort`), with Spark as the default,
deterministic pass-through for omitted stages, and Playtest evidence required
only by Quest.
