- Bind the host CAD wall-thickness gate to the product's sealed print-readiness
  claim. An explicit `digitally-verified-not-print-ready` revision can now
  retain the deterministic generation, fit, spec, motion, kernel, export, and
  mesh gates without being blocked by thickness or represented as print-ready.
