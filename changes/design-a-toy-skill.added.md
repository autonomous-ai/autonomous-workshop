- Add the `design-a-toy` Claude Code skill: design a toy with an Inventor before
  any run starts, gate the spec before generating one reference image per unique
  geometry, and emit the `workshop wish` command so Make builds a finished design
  instead of inventing one.
