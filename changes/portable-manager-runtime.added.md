- Add `workshop wish --manager {codex,claude,grok}` so a run freezes one native
  Manager runtime. Codex remains the default and production path. Claude Code
  and Grok Build are experimental adapters to the same host gates; resume
  cannot switch Managers.
