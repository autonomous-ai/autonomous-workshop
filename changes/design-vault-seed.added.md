- Add the design vault: `workshop.invent.vault` reads, lints, packs, and
  queries an Obsidian-compatible typed-link graph of mechanisms, failure
  modes, and recorded fixes; a cleaned seed ships under
  `src/workshop/invent/vault/`; `workshop vault seed|lint|check` manages the
  host-owned copy at `$WORKSHOP_HOME/vault/`.
