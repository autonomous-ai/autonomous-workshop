- Name products like products. The Make and Release references now say that
  `title` and `summary` are customer copy: a sayable one-to-four-word name
  with no dimensions, part counts, or sentences, and no Workshop vocabulary
  (Wish, Taste, Goal, Make, Release, Playtest, Spark, Forge, Quest, artifact,
  gate) anywhere a shopper can read it.
- Point published pages at the shop's current home: product URLs are
  `autonomous.ai/toys/product/<slug>` instead of the redirecting
  `autonomous.ai/factory/product/<slug>`.
