- Validate required Make `product.json` metadata before finalization and resume
  the same Codex session with hash-bound repair feedback when an older valid
  proposal fails the trusted Make contract, with a timeout-capped completion
  grace after Codex writes a stage proposal.
