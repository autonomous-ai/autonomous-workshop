- Keep bundled CAD helpers importable in final verification's local fit/spec
  audits, including isolated rebuilds. The verifier resolves its materialized
  scripts directory explicitly; invalid audit results still fail verification.
