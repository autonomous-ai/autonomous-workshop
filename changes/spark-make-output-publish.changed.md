Spark selects its inventor in Workshop setup, keeps Make unchanged, and publishes
Make's existing files without another native turn, CAD rebuild, manual generation,
or review. Explicit inventor choices bind immediately; automatic selection uses
the same native session. New publication receipts bind the exact uploaded metadata
anchor and truthfully record omitted reviews. Existing authentication, ownership,
idempotency, exact file integrity, and public readback remain required. Deterministic
acceptance passes; the live chess publication is still in progress.
