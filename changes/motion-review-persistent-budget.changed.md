- New product runs review coupled mechanisms using hash-bound exact-state
  animation alongside the existing engineering checks and distinguish
  revisable Spark styling from user requirements (ADR 0047, still in force).
  Unknown nonzero native exits no longer trigger unfinished-turn retry loops,
  and the isolated PDF checker keeps the canonical runtime's dependencies
  without working-directory or user-site imports.
- History (superseded): the persistent native execution allowance this change
  introduced was replaced by persistent turn budgets (ADR 0048) and then by
  one product-wide token budget (ADR 0049); frozen older runs retain their
  materialized protocol.
