Make overhang reports reproducible across CAD-project relocation by recording
the mesh basename, exact mesh hash and effective check parameters instead of
absolute invocation paths. Measurements and pass/fail checks are unchanged.
Refresh the reviewed CAD skill fingerprint; frozen runs retain their old copy.
