- Require Playtest to answer every issued design-vault lead once in the
  `agent-playtest` observations (`confirmed` naming the feedback that repairs
  it, or `dismissed` with a reason); the finalizer and the host gate both
  enforce it and the gate receipt records the counts.
