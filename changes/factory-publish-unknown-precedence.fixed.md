Factory publication status now gives an ambiguous durable publish intent
precedence over an older local draft receipt, and resumes reconcile that same
idempotent intent without blindly sending publication twice.
