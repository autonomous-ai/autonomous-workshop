# Spark Factory handoffs reuse Make's sealed hero as their cover

Spark Make-output Release now aliases the one sealed `snap/iso.png` hero into
Factory's recognized cover path. STEP-only imports therefore have a cover
without reintroducing host rendering, mutating Made, or exporting a mesh.
