Fixed: Codex product runs now give the bundled Step.parts downloader a limited,
domain-allowlisted network proxy, so `workshop wish` and `workshop resume` can
source checksum-verified component CAD without manual host file injection.
