Fix fresh CAD gate rejection caused only by directory paths embedded in
`measure/overhang-*.md`. Normalize the two known path-bearing metadata lines;
keep every option, measurement, check result, region and other byte exact.
Geometry, source, file-mode and publication checks remain unchanged.
