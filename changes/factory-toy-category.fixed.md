- Send Factory's canonical `toys` category on Workshop imports and require the
  exact category in authenticated draft and publication readback instead of
  accepting Factory's first-active-category default.
