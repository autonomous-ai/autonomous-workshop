Keep no-dependency wheel acceptance scoped to package inventory and CLI
discovery, and run the native Wish smoke only in the dependency-complete
acceptance tier where its declared process supervisor is installed.
