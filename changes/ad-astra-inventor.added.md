- Add the `ad-astra` Inventor: impact-first astronomy reskins of public-domain
  board games with no source-popularity gate, rules frozen, physical affordances
  free while the legal-move set is unchanged, and exactly one `signature`
  component exempt from the state-legibility floor.
- Register `ad-astra` and the previously missing `arden-span` in
  `BUNDLED_INVENTOR_IDS` so both ship with the package.
