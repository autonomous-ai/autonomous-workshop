- Record cached, uncached, cache-write, reasoning, and non-reasoning token
  telemetry by native stage when Codex reports the complete breakdown, while
  keeping older gross-only token snapshots readable without guessed values.
