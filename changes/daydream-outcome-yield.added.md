- Summarize Daydream outcome memory as host counts: `NOTEBOOK.md` now opens
  its outcome section with how many theses reached a build, how many
  published, where the rest stopped by stage, and how many sessions errored or
  were interrupted, split by route. `workshop daydream <inventor> --outcomes`
  prints that view without dreaming (`--json` adds `summary` and every
  `outcomes` event). Counts are facts over each run's latest event, not a
  score; they make the retired predictive Judge's replacement measurable.
