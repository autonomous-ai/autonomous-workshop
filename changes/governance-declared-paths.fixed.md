- Correct `.github/components.toml` and its CODEOWNERS projection so every
  declared source and test path exists in the repository (`tests/integration`,
  `tests/inventors`, `tests/fixtures`, and `web` were never checked in).
