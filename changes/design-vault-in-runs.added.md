- Snapshot the design vault into every new product run as the read-only
  `design-vault` skill (`vault.json` + `vault_tools.py`); Invent concepts must
  resolve their mechanisms to vault nodes or declare them novel, declared
  conflicts and unmet requirements are refused by the finalizer and the host,
  and Make/Playtest packets carry the vault's `vault_leads`. Runs that
  predate the vault are unchanged.
