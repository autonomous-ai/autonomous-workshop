- Seal Invent concepts as Invented schema 4 with a deterministic contract:
  required `interaction`, `envelope_mm`, `mechanisms`, and `components`
  fields, and finalizer/host rejections named `unbound`, `envelope`,
  `component-orphan`, `signature`, and `decoration`. Schema 3 contracts in
  sealed runs remain readable and resumable.
