# Prepared current-geometry batch — not executed

After the Manager completes the changed component passes and regenerates the 44-leaf assembly, run from the workspace:

```bash
bash artifacts/make/r0001/visual-repair-geometry/run-batch.sh
```

The runner records current source/STEP hashes at execution and rejects the old assembly STEP. It makes 21 documented native inspection requests: refs/facts/planes/positioning and full validate for gear14, gear15, gear23, carrier, coupon and retained gear18/20/22/26, then refs, full validate and unrestricted interfere for the assembly. No prepared hash is used to assume future export identity. No max-pairs cap, restricted references, skipped self-intersection check, tolerance relaxation or cache edits.

Inspect every JSONL response's ok/exitCode/result, not only shell exit. Confirm 44 leaves, 946 possible assembly pairs, no truncation, and unchanged default 1 mm³ interference tolerance. Reconcile returned reference STEP hashes with execution-manifest.json/current generated bytes. A measured defect fails; a formal native exhausted/cancelled result remains UNVERIFIED and must be preserved. This does not replace integrated verification, motion, print or independent review.

Twelve unchanged component STEP files currently match the prior full-validation evidence exactly: axle, bezel, foot_trim, gear_cap, main_cap, orange_disc, pedestal, pedestal_lid, rim_trim, stem, sun and support_collar. Their prior raw validation responses completed successfully and the old summary/manifests remain hash-consistent. The four retained planet exports differ in bytes, so they are explicitly included in this primary batch despite intended equivalent forms. Old16/24 forms are retired, not current-production requirements. Current assembly placement and source generation are never inferred solely from old component bytes.

## Supplemental feature evidence needed after generation

- New14/15/23 gear webs: bind current params, gearing/gear builders, entry wrappers and each exact master STEP. Read the actual top-face inner wires from each exported master, distinguish its bore from five swept windows, export five actual window voids, then measure all five cyclic adjacent boundary pairs using the previously used public build123d nearest-distance API. Preserve each void STEP hash, both closest points and the complete 15 raw distances; compare against 2.4 mm. Source sampling is not the measured result, and generic 0.8 mm wall PASS is not the 2.4 mm web proof.
- Current total is eight distinct forms (14/15/18/20/22/23/26 plus sun), hence 40 distinct-form webs. The unchanged sun's old five measurements can be rebound through exact master bytes. Retained18/20/22/26 currently differ in STEP hashes: establish precise current-to-old geometric equivalence or measure their twenty webs anew; do not simply rename old source bindings. Old16/24 measurements remain historical.
- Exact nominal sightline: after new assembly export, bind its current hash and source. Preserve original 44 leaf solids and full world placements in a grouped compound without union; assert 44 original solids plus one witness. Use radius7.00 mm, axis X0/Z120, Y=-200..200, and native `inspect interfere <new-witness-entry.step.py> --tolerance 0.000001`. It must test the single all-product-versus-cylinder pair without skips/truncation/errors. Read refs for the exact witness and source identity. The historical13.98 mm witness and the old40-leaf nominal14 mm result do not prove the current carrier aperture.
- Keep all new witness sources/results under a new scope. Preserve `final-feature-probes/`, `geometry-final-batch/` and the old measure/audits bytes. Neither supplemental probe is included in requests.jsonl yet because it needs freshly generated, exactly bound witness geometry and references. No feature probe or CAD operation ran in this preparation task.
