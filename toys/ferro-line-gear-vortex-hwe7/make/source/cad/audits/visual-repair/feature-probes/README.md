# Prepared feature probes — not executed

All files in this directory are a new scope. Historical final-feature-probes, product artifacts and evidence are untouched. No CAD, boolean or nearest-distance query ran during preparation.

After current component generation completes and no heavy job is running:

```bash
bash artifacts/make/r0001/visual-repair-feature-probes/run-web-measurements.sh
```

This checks the eight pinned master hashes, uses native gen for actual exported-window voids from14/15/18/20/22/23/26 and36T sun, then calls the public build123d nearest-distance API. The copied method identifies one central bore and five actual inner windows on the current top planar face; it measures all five cyclic window-boundary pairs, including last-to-first. It records40 raw nearest distances, both witness points, window descriptors, master hashes and void STEP hashes. Compare every measured distance against2.4 mm; do not treat source sampling or generic wall thickness as that proof. If a master hash changes before execution, rebind honestly rather than bypassing the assertion. No old gear measurements transfer in this task.

After the new44-leaf assembly is generated, separately and serially:

```bash
bash artifacts/make/r0001/visual-repair-feature-probes/run-sightline.sh
```

The binder records exact current assembly and source hashes immediately before native generation and rejects the old40-leaf export. The witness imports those pinned bytes and asserts44 original leaf solids, preserves every world placement, groups without union, and adds one radius7.00 mm cylinder at X0/Z120 over Y=-200..200. Native refs must identify45 shapes within two leaf groups. Native interference must test the one complete-product-versus-cylinder pair at unchanged0.000001 mm³ tolerance, skip/truncate none, and report no clash/error. Check each JSONL ok/exitCode; an outer process exit alone is insufficient.

The shared helper differs from the prior method only in original-solid count40→44 and exact witness radius6.99→7.00. The nearest-distance query is byte-identical to the prior public-API query. The sightline is nominal-pose clearance only and does not replace full assembly interference, motion or physical light transmission. A preparation status is not a PASS. Native failures/UNVERIFIED outcomes must be preserved as such. No tool, cache or threshold was modified.
