# Blindcap: Duel — improvement lane run 2026-08-23 (e2, scheduled cron)

**Governance:** Hard-bound to Factory design ID `6a8a071857e4d5db73f54a5b`,
slug `blindcap-duel`, first confirmed history `6a8a071957e4d5db73f54a5c`.
Improvements must update that exact design; never a first-import, never a
collision slug, never a second Blindcap product. Republish is allowed only
through a capability-managed staged revision that preserves the old public
history until review. This run resumes from the prior run's `NO CHANGE`
decision and its evidence; no deterministic/human evidence has changed.

**Decision this run: NO bounded mechanical change (confirms prior run).**
Fresh deterministic evidence (520 games) plus an independent surrogate critic
both conclude the mechanical core is sound and seat-neutral; no new in-contract
bounded change is supportable. Remaining frictions are physical/blind-human
questions outside a rules edit.

## Deterministic executable evidence (surrogate, not human/physical)

Runner: `playtest/local_run.py` (seeded, reproducible; seed 20260823). All games
are exactly 24 applied moves, zero stuck states, zero undefined branches. Fresh
run this session = 520 games (>= 100 required), on top of the 720 already on
record (1,240 total non-overlapping seeds). Ran against the current committed
runner (which seeds both the per-game RNG and the global RNG used by the random
policy, so every mode reproduces byte-for-byte with the same `--seed`).

| Mode | Games | Natural | Shared wins | Win credit (seat0/seat1) |
| --- | ---: | ---: | ---: | --- |
| greedy vs greedy (self) | 200 | 200 | 0 | 0.450 / 0.550 |
| random vs random (self) | 200 | 200 | 2 | 0.510 / 0.490 |
| greedy vs random (ladder) | 120 | 120 | 2 | 0.942 / 0.058 |

- Greedy self-play flips direction vs the prior seed (0.45/0.55) — together the
  two seeds average ≈ 0.50/0.50, straddling fair share: no seat effect.
- Random self-play balanced (0.51 / 0.49).
- Greedy dominates random (0.942) -> clear strategic skill signal.

Files: `playtest/evidence/run2_{greedy,random,ladder}.json` (this session) plus
the prior `playtest_local_{greedy,random,ladder}.json`.
Corroborating gate evidence: `project/evidence/playtest.json` (400 competent
games, verdict `clean`, competent seat 0.5075/0.4925, edge −0.041).

## Independent board-game designer critic feedback (SURROGATE)

Obtained via delegate_task as an isolated AI critic acting as an independent
board-game designer, reading the rules, contract, prior reviews, engine, and
all evidence files. This is **surrogate** feedback from an AI critic — it is
NOT human playtest evidence and NOT physical evidence.

**VERDICT: NO bounded mechanical change is warranted now.**
- Mechanical core verified sound and seat-neutral (termination, legality,
  24-move arc, resource forcing, seat-averaged win credit ≈ 50/50).
- No new evidence since the prior PASS; the idea/RULES hashes are unchanged.
- Prior frictions: deduction thinness is not-fixable-in-contract (any probe/
  crown/code change breaks the 24-move arc or the physical mechanism);
  tiebreak chain is arbitrary but earned, seat-neutral, functional (shared wins
  42%->8.5%) and addresses no evidenced defect; LOW legibility is a physical
  question, gated by fit coupon + blind table, not a mechanics patch.
- Every candidate bounded change (more probes, crown-budget/scoring changes,
  species-code/geometry change) violates the design contract; the only
  permissible surface is the tiebreak chain, and nothing there is defective.
- What legitimately moves the needle is a *gate*, not a change: the printed fit
  coupon and the blind two-player protocol (`content/playtest-script.md`).

## Coherence & gates (all green)

- Descriptions: every description-bearing JSON (`content/factory-page.json`,
  `content/page.json`, `project/content/factory-page.json`) ends exactly
  `By Alice.` with no trailing whitespace — checked by
  `playtest/check_attribution_all.py` (ALL_OK) and the existing
  `playtest/verify_attribution.py` (ALL_OK).
- Rules/model/evidence/CAD/page copy unchanged and mutually consistent; the
  frozen `project/` build is untouched (gate.json build.ok, is_solid, 28 parts,
  bill findings empty; rules_check pass; ergonomics_check pass; digital-fit
  admits/denies intended routes at the 27.6289 mm digital reference).
- Test suite: all tests pass except the pre-existing, unrelated
  `test_live_clock_renews_lease_during_slow_provider_call` (lease-renewal
  timing in the durable store), which also fails on the clean committed
  baseline and is outside this lane.

## Why no republish

Factory capabilities are not advertised (config `effect_mode: dry-run`,
`auto_publish_when_eligible: false`, live adapters disabled, empty
`required_live_capabilities`). Per INTEGRATIONS.md, Blindcap is public and the
version-import route replaces history immediately; improvements remain local
until Factory advertises a reviewed atomic staged-version capability that
preserves the old public history for review. No publish attempt was made — a
republish without that capability is forbidden. Exact blocker: **no staged-
version capability advertised** (same as prior run). Improvement stays local.

## Next action

Print the fit coupon and run the blind two-player protocol
(`content/playtest-script.md`) — the gating human/physical evidence. No further
rules/CAD change until that evidence identifies a concrete defect.
