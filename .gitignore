# local env + creds (only inert examples belong in Git)
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example
*.pem
*.key
*.p12
*.pfx
**/*.bak
**/*.bak-*
**/*.bak.*
**/state/portal-auth.json
**/state/panda-auth.json

# generated runtime state
.runtime/
**/.workshop/
.cadgen-budget.json
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.sqlite3
*.sqlite3-shm
*.sqlite3-wal

# python
__pycache__/
__cadgen__/
*.pyc
*.egg-info/
.pytest_cache/
.coverage
.venv*/
/build/
/dist/

# macOS
.DS_Store
.venv-cad/

# Private runtime workspaces never belong in Git.  The source-checkout
# `toys/<inventor>-<slug>/` namespace is reserved for sanitized public examples,
# so those snapshots are visible without a per-product allowlist.  Old
# path-bound Wish workspaces remain ignored and resumable in place.
/runs/
/state/
/credentials/
/toys/wish-*/
/toys/.public-example-*/
/tmp/

# Private planning material (not published)
/docs/inventor-army/
/mockups/
