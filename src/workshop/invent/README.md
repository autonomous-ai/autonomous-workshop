# Invent

Owns only the deterministic `Invented` handoff contract. Codex performs native
search, source evaluation, concept exploration, synthesis, and specialist
delegation in the product project; this Python package does not implement a
research agent, prompt chain, reward loop, or stage orchestrator.

Public API: `workshop.invent`.
