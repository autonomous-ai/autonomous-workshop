# Blindcap: Duel — improvement lane run 2026-08-23 (Alice)

**Governance:** Hard-bound to Factory design ID `6a8a071857e4d5db73f54a5b`,
slug `blindcap-duel`, first confirmed history `6a8a071957e4d5db73f54a5c`.
Improvements must update that exact design; never a first-import, never a
collision slug, never a second Blindcap product. Republish is allowed only
through a capability-managed staged revision that preserves the old public
history until review.

**Decision this run: NO bounded mechanical change.**
Evidence (720 deterministic executable games + independent surrogate critic)
does not support a rule edit. The validated core is complete and seat-neutral;
every remaining friction is a physical-coupon-fit or blind-human-play question
that no bounded rule change can resolve. Forcing a speculative edit would
violate the lane gate ("choose one bounded improvement only when evidence
supports it") and would break the frozen `project/` build without a way to
republish.

## Deterministic executable evidence (surrogate, not human/physical)

Runner: `playtest/local_run.py` (seeded, reproducible — the runner seeds both
the per-game RNG and the global RNG used by the random policy, so every mode
reproduces byte-for-byte with the same `--seed`). All games are exactly 24
applied moves, zero stuck states, zero undefined branches, all four move kinds
used. 720 games total (>= 100 required).

| Mode | Games | Natural | Shared wins | Win credit (seat0/seat1) |
| --- | ---: | ---: | ---: | --- |
| greedy vs greedy (self) | 400 | 400 | 0 | 0.542 / 0.458 |
| random vs random (self) | 200 | 200 | 0 | 0.485 / 0.515 |
| greedy vs random (ladder) | 120 | 120 | 1 | 0.954 / 0.046 |

- Greedy self-play is seat-balanced (0.542, close to fair share).
- Random self-play is seat-balanced (0.485).
- Greedy dominates random (0.954 vs 0.046) -> clear strategic skill signal.

Files: `playtest/evidence/playtest_local_{greedy,random,ladder}.json`.

## Independent board-game designer critic feedback (SURROGATE)

Obtained via delegate_task as an isolated AI critic acting as an independent
board-game designer. This is **surrogate** feedback from an AI critic — it is
NOT human playtest evidence and NOT physical evidence. It does not replace the
mandatory blind two-player blind tables and printed fit coupon.

**VERDICT: No change warranted.**
- The executable evidence validates a mechanically sound, seat-neutral core.
- Every concrete friction (score legibility, tactile reward, "deduction feels
  thin") is a human/physical question no bounded rule change can resolve.
- The honest blocker is blind human play + printed physical fit, not a design
  flaw.

Critique highlights:
1. Strongest idea is the multi-use probe (answer + public memory + scarce
   resource in one element).
2. The 3-probe / 2-early-crown budget is airtight: exactly fills five action
   slots, priced info-vs-commitment, no pass rule needed.
3. Genuine contest conflict: converting a rival's n² grove into an n-split.
4. Seat neutrality is evidence-backed.
5. Weakness: deduction depth genuinely thin (3 probes resolve ~1 of 6 rival
   mushrooms); unfixable by rule without breaking the 24-move arc.

## Incoherence / friction flagged (not blocking this run)

- The LOW tiebreak loads score + tactile legibility onto the least-legible
  physical state (3 mm near-flush reading); monitor at physical test.
- Crown-inversion "you score groves you colonize, never groves you grow" is
  the least-intuitive rule; keep the "aha" line explicit on the aid.
- The 5-level tiebreak chain is arbitrary but harmless as a decider.

## Why no republish

Factory capabilities (`REQUIRED_LIVE_CAPABILITIES`) are not advertised; there
is no atomic staged-version capability that would preserve the old public
history for review. `config/default.json` has `effect_mode: dry-run` and
`auto_publish_when_eligible: false`; all live adapters are disabled.
Per INTEGRATIONS.md, Blindcap improvements remain local until the backend
advertises a reviewed staged-version capability. No publish attempt was made —
a republish attempt without that capability is expressly forbidden.

## Next action

Print the fit coupon and run the blind two-player protocol
(`content/playtest-script.md`) — the gating evidence. No further rule/CAD
change until physical/human evidence identifies a concrete defect.
