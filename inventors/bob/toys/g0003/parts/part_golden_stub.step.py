"""golden_stub — CLEARANCE (g0003) TEST FIXTURE, not a shipped part.

Contract: ../brief.md, "Physics question to prove first (golden part)".
Print this with part_column_screw and part_detent_leaf and run tests 1-3
before committing the 34 h production plate.
"""

import clearance_lib as lib


def gen_step():
    return lib.print_golden_stub()
