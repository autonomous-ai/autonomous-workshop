## 1. Establish the Versioned Protocol Baseline

- [x] 1.1 Verify `restore-dormant-concept-contracts` is integrated by running its focused Concept, schema-discovery, architecture, packaging, and strict OpenSpec validation checks; stop if schema-v2 paths or identities differ from this design.
- [x] 1.2 Add failure-first workflow tests for a frozen `invent-concept-v1.md` capability: marked Forge/Quest must select compound Invent behavior, while Spark and unmarked historical fixtures retain their exact packet, artifact, and gate shapes.
- [x] 1.3 Add failure-first packet and stage-subject tests for initial and revised marked Invent attempts, including canonical Concept paths, marker hash, prior sealed Concept, effect evidence, round, and exact revision bindings.
- [x] 1.4 Add failure-first contract tests for the marked Made schema with Concept/effect identities and prove the existing schema-v1 reader and hash shape remain unchanged.
- [x] 1.5 Add topology regression tests proving no `concept` stage, Goal, turn, transition, checkpoint, pass-through artifact, or status value is introduced for any effort route.

## 2. Materialize Compound Invent Authorship

- [x] 2.1 Add the immutable `references/invent-concept-v1.md` capability and register it in product-run asset/package manifests; verify source-tree and installed-package tests reproduce its exact bytes and hash.
- [x] 2.2 Extend the marked Invent `STAGE.json` inputs with the packet-bound Concept root, pre-render/sealed/effect paths, and current revision context; verify packet tests reject unsafe, non-canonical, missing, or capability-mismatched fields.
- [x] 2.3 Extend the run-local `invent` finalizer with packet-gated pre-render input handling that derives v2 Invent provenance from the exact assignment, Invented, creative-source, Wish, round, and revision bytes; verify finalizer tests reject every substituted identity and stale round.
- [x] 2.4 Make the run-local finalizer load and structurally validate the exact five-file pre-render tree, write canonical `pre-render.json`, and list the complete authored artifact set in one outcome; verify missing roles, incomplete attribution, placeholders, duplicate paths/keys, links, special nodes, extra source files, and post-read drift fail before outcome creation.
- [x] 2.5 Preserve finalizer portability without importing builder-only workflow/runtime/effect code; verify installed no-dependency finalizer acceptance succeeds inside a materialized toy project.
- [x] 2.6 Update product-run `AGENTS.md`, the autonomous-workshop skill, and `references/invent.md` with the compound Concept authoring sequence and one-finalizer stopping condition; verify skill-lock and package snapshot tests bind the revised exact instructions.
- [x] 2.7 Add Invent instruction tests proving the Manager and selected Inventor own research/design content while the finalizer performs no research, prompt composition, rendering, credential access, semantic judgment, or lifecycle transition.

## 3. Add Pre-Effect Host Validation and Pending Invent State

- [x] 3.1 Extend the routed Invent host gate to reopen the exact compound artifact set and repeat assignment, Invented, creative-source, provenance, round/revision, source-manifest, derived-Wish, and structural checks; verify post-finalizer mutation fails before any effect intent exists.
- [x] 3.2 Add a bounded Invent proposal-rejection record and same-session feedback path modeled on current Make/Playtest recovery; verify malformed agent-authored bytes are quarantined, counted independently, and never consume the lifecycle revision budget.
- [x] 3.3 Add a private pending-Invent-proposal contract bound to checkpoint, subject, outcome, pre-render Concept, and exact artifact hashes; verify tampered, stale, symlinked, duplicate, or cross-run pending state fails closed.
- [x] 3.4 Make resume process a valid pending Invent proposal before launching Codex; verify successful effect continuation does not create another native turn or repeat Invent cognition.

## 4. Implement the Durable Concept Image Effect

- [x] 4.1 Define bounded Concept aggregate/role intent and sanitized receipt contracts under the trusted runtime boundary; verify canonical serialization, finite JSON, safe text, exact hash bindings, and strict field rejection.
- [x] 4.2 Implement the host-private Concept effect ledger with `planned`, `sending`, `succeeded`, `rejected`, and `unknown` role states plus stale transition tokens and immutable audit history; verify duplicate preparation is idempotent and changed inputs cannot reuse an identity.
- [x] 4.3 Add aggregate completion rules requiring the exact descriptor role set and exact installed byte hashes; verify partial, extra, duplicate, changed, or cross-Concept role receipts cannot produce success.
- [x] 4.4 Version the run authorization record with disclosed marked Forge/Quest Concept-render authority while preserving Factory/Git fields and old readers; verify Spark has no authority and missing/mismatched authority prevents credential loading and network calls.
- [x] 4.5 Add a private Concept-image credential/profile loader that is invoked only after the native turn exits; verify subprocess environments, readable permission roots, packets, proposals, status, logs, archives, and test fixtures never contain credentials.
- [x] 4.6 Implement the narrow pinned-origin production image adapter and injected transport boundary with explicit profile/model/request schema, one final image, ordered inline references, media sniffing, size/time bounds, redirect refusal, and bounded provider metadata; verify immediate success plus every malformed, oversized, cross-origin, and pre-transmission failure case.
- [x] 4.7 Implement provider-capability-aware ambiguity classification and reconciliation; verify safe pre-transmission/absence retries reuse the same intent, durable operation ids use authenticated readback, and an unreconcilable post-transmission disconnect remains unknown without resend.
- [x] 4.8 Implement deterministic role planning from validated drawing instructions and completed reference hashes; verify `front`, dependent overall roles, `exploded`, and stable component roles execute in the declared order without host prompt edits or undeclared references.
- [x] 4.9 Atomically validate and install returned image bytes at descriptor paths using host-private temporary files; verify links, special nodes, path escape, conflicting existing bytes, changed parents, and unexpected files fail closed while identical reconciled bytes are idempotent.
- [x] 4.10 Seal the complete tree with the existing pure v2 boundary, write canonical `sealed.json` and sanitized `effect.json`, then independently rehash both before the Invent gate; verify private operation ids, raw responses, credentials, and diagnostic text never enter workspace artifacts.
- [x] 4.11 Add Invent effect wait/resume handling for missing configuration, transient safe failures, partial progress, and unknown outcomes; verify checkpoint/session/proposal identity survives command exit and status exposes only a bounded safe `invent` need.

## 5. Make the Sealed Concept Load-Bearing

- [x] 5.1 Implement the marked Made schema with exact `concept_sha256` and sanitized `concept_effect_sha256` bindings while retaining schema-v1 compatibility; verify canonical round-trips and every substituted upstream identity fail.
- [x] 5.2 Extend marked Forge/Quest Make packets and subjects with the full sealed Concept, artifact/effect bindings, derived Wish, brief, descriptor, and image manifest; verify Spark and unmarked packet snapshots remain byte-for-byte compatible.
- [x] 5.3 Extend the marked Make finalizer to reopen the sealed Concept/effect evidence, require its packet identities, and emit the marked Made contract; verify missing, stale, cross-round, changed, or unmarked Concept input is refused.
- [x] 5.4 Require product metadata to declare stable component keys and add exact equality checks against the Concept brief in both finalizer and host gate; verify missing, extra, duplicate, renamed, and ambiguous counterparts fail by key without a model call.
- [x] 5.5 Add whole-product Concept-pixel exclusion using the exact Concept image hashes; verify identical bytes fail at any depth/name while independently rendered product images and visually similar bytes remain eligible.
- [x] 5.6 Rehash the complete sealed Concept and sanitized effect evidence at the trusted Make boundary and carry their identities through downstream Made context checks; verify tampering after packet creation rejects Make before CAD acceptance.
- [x] 5.7 Extend Make-to-Invent revision evidence, Quest fundamental revision context, invalidation, and round archival with exact standing Concept/effect identities; verify Concept defects return to Invent, CAD defects stay in Make, and only the existing shared revision budget is consumed.
- [x] 5.8 Extend Playtest and Release upstream context checks for the marked Made identity without admitting Concept art or research as evidence; verify current signature-review, Playtest, print-ready CAD, manual, Factory, and truthful omission gates remain unchanged in claim strength.

## 6. Preserve Compatibility, Privacy, Packaging, and Archives

- [x] 6.1 Add a complete marked/unmarked compatibility matrix covering new Forge/Quest, Spark, pre-marker effort runs, pre-effort runs, historical Made contracts, waits, and resumes; verify installed upgrades never synthesize Concept requirements for frozen protocols.
- [x] 6.2 Add architecture/import-policy tests proving Concept contracts/finalizer checks remain free of workflow, runtime, integration, credential, network, and cognitive orchestration imports, while effect orchestration remains host-only and credentials remain integration-owned.
- [x] 6.3 Extend private run archival and optional public toy snapshot composition for active Concept artifacts; verify exact private contracts/images are retained, public copies are sanitized under existing authority, and provider-private state, raw receipts, credentials, private Wish context, and Concept pixels never enter Factory product evidence.
- [x] 6.4 Update schema registry, package data, sdist/wheel contents, installed no-dependency acceptance, and materialized asset hashes for all new contract/marker/instruction files; verify source and installed bytes match exactly.
- [x] 6.5 Run secret, permission-profile, network-boundary, artifact-path, and out-of-root-write policy tests; verify the native subprocess cannot read host Concept credentials/effect state or invoke the image adapter directly.

## 7. Prove End-to-End Behavior

- [x] 7.1 Extend the deterministic external-runtime executable to author valid compound Invent source for marked Forge/Quest and add a deterministic image transport double only at the outbound adapter boundary; verify one Invent turn seals Concept and transitions directly to Make.
- [x] 7.2 Extend deterministic E2E happy-path assertions for marked Forge and Quest through Concept-bound Make, current Playtest/direct-Release rules, Factory doubles, and terminal publication; verify stage traces contain no Concept lifecycle event and Spark remains `Make -> Release` without provider calls.
- [x] 7.3 Add deterministic E2E failure scenarios for source mutation, partial roles, missing authority/credentials, provider rejection, pre-transmission timeout, ambiguous post-transmission outcome, authenticated reconciliation, stale receipt, changed sealed bytes, Made mismatch, component mismatch, copied pixels, and stale re-Invent input; verify each exact wait/rejection/invalidation outcome.
- [x] 7.4 Extend the mock-session harness and context-proof records so a real persistent Codex session authors and finalizes pre-render Concept during the existing Invent Goal, then resumes the same session at Concept-bound Make after host effect doubles complete; verify no host-internal mock or second root session is introduced.
- [ ] 7.5 Add an opt-in real-Codex wait/resume acceptance case with partial completed roles and reconciliation; verify status privacy, unchanged session/checkpoint/proposal identities, no repeated Invent cognition, and exact final receipts.

## 8. Documentation and Final Verification

- [x] 8.1 Update `docs/NATIVE_AGENT_RUNTIME.md`, `docs/ARCHITECTURE.md`, `docs/CONCEPT_PHASE_INTEGRATION_PLAN.md`, Concept/Invent/Make component docs, evidence docs, and public-archive docs to describe implemented Forge/Quest activation, Spark deferral, effect waits, exact bindings, and non-evidentiary image claims.
- [x] 8.2 Add a change note recording exact implementation and verification commands without claiming aesthetic quality, buildability from Concept art, physical manufacture, delivery, or live-provider success from deterministic doubles.
- [x] 8.3 Run focused Concept, Invent, Make, workflow, runtime-effect, integration, CLI, archive, architecture, and packaging unit suites with `PYTHONPATH=src python -m unittest`; verify all failure-path coverage passes.
- [x] 8.4 Run the required deterministic route matrix with `WORKSHOP_RUN_DETERMINISTIC_E2E=1`, then run the opt-in real-Codex marked Forge/Quest acceptance in an authorized environment; record exact stage/session/effect evidence and any skipped live-provider condition truthfully.
- [x] 8.5 Run the full offline unittest suite, build/install acceptance, strict validation for both the prerequisite and this change, secret scan, and `git diff --check`; verify every check passes before exposing the activation marker in a release.
- [x] 8.6 Expose complete field-by-field canonical skeletons for all five
  agent-authored Concept JSON inputs, including nested constraints, cross-file
  bindings, role ordering, descriptor sealing timing, and canonical hash
  derivation; add a source-asset regression test and reconcile the active
  proposal, design, and Invent Concept specification with that contract.
- [x] 8.7 Expose the marked Make component inventory directly in its packet and
  routed `product.json` skeleton, distinguish Concept components from
  implementation-specific CAD variants and repeated physical instances,
  reconcile protected empty-cache guidance, and add
  regression coverage that does not pre-bake the hidden schema.
