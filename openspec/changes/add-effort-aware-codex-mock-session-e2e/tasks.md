## 1. Acceptance Evidence and Isolation

- [x] 1.1 Port the bounded mock-session context-record contract into test-only code, bind it to current stage-packet, checkpoint, subject, instruction, input, strategy, deferred-work, and output fields, and verify focused tests reject duplicate keys, malformed paths, stale bindings, missing files, oversized records, and symlinks.
- [x] 1.2 Implement final-byte and run-root-only validation from `69afa5c` and `e4eb604`, comparing context records with turn-time packet snapshots, finalizer proposal inventory, accepted manifests, and final source hashes; verify mutations after proof creation and checkout/harness path citations fail by name.
- [x] 1.3 Add mock-session dependency and replacement policy guards that keep helpers out of `src/`, `src/cli/`, and `.agents/product-run/` and allow only the Codex executable plus outbound Factory transport seams; verify representative launcher, finalizer, gate, verifier, checkpoint, and effect-coordinator replacements are rejected.

## 2. Transparent Real-Codex Pass-Through

- [x] 2.1 Adapt the test-only pass-through executable so version checks, native start/resume arguments, stdin, JSONL events, stderr, exit status, and termination signals reach the resolved real Codex child unchanged except for one bounded generic overlay; verify forwarding with deterministic child-process tests.
- [x] 2.2 Define and statically audit the versioned overlay so it requests minimal context-derived work and the generic proof record without including stage schemas, finalizer subcommands, route names, transitions, folded-selection rules, omission rules, or ownership recipes; verify every forbidden workflow fragment fails the audit.
- [x] 2.3 Capture native turn packets, final output hashes, model/effort bindings, session identifiers, and prohibited event types; verify focused tests fail on missing context proof, changed source bytes, web activity, non-loopback activity where observable, credential solicitation, unnecessary subagent events, and session-id drift.
- [x] 2.4 Exercise the wrapper through the production native launcher with a deterministic child for start/resume protocol characterization, then add live preflight assertions that the acceptance scenario resolves a different real Codex binary, creates a production session checkpoint, and never reports a scripted child as live acceptance.
- [x] 2.5 Temporarily release control after a new exact regular finalization marker is safely reaped, or boundedly resume an already checkpointed exact session with an observed valid identity and no proposal, when `turn.completed` is absent; retain normal host proposal gates, test every fail-closed boundary, and document the unconfirmed compatibility issue and removal criteria under `docs/backlog/`.

## 3. Current Factory Publication Fixture

- [x] 3.1 Replace the historical Concept/private-Deliver protocol fixture with a loopback-only Factory fixture implementing the current import, metadata, publish, authenticated design readback, and public project-file readback contracts; verify unexpected methods, paths, authentication, or duplicate effect behavior fail focused protocol tests.
- [x] 3.2 Route the fixture only through the existing outbound Factory transport seams while leaving production credential parsing, intent/idempotency ledger, release writer, reconciliation, public transition, receipt validation, and terminal checkpoint code active; verify an end-to-end deterministic harness test observes every expected production operation.
- [x] 3.3 Bind the fixture's public file response to the exact sealed manual bytes received during import and verify altered CAD, package, page, manual, project, or public-readback bytes prevent terminal Release.
- [x] 3.4 Add host/agent write inventories and recognizable canary credentials, then verify the fixture writes no workspace or host-state artifact and secrets are absent from the native environment, prompts, agent files, context records, reports, and redacted diagnostics.

## 4. Effort-Aware Live Harness

- [x] 4.1 Adapt the isolated harness and tool entry point with supported-Codex/authentication/fixture preflight, explicit `--effort`, a fixed simple Wish, private temporary Workshop home, per-turn and whole-route budgets, owned-process cleanup, `--keep`, redaction, retained failure manifests, and nonzero prerequisite/timeout results; verify each lifecycle and failure path with offline harness tests.
- [x] 4.2 Implement the Spark live scenario through production `start_native_run`, finalizers, gates, CAD/PDF verification, sealing, Factory publication, and public readback; verify its trace is exactly Make -> Release, Make contains folded assignment and Invented provenance, Playtest residue is absent except canonical `not-run`, and one session is started then resumed once.
- [x] 4.3 Implement the Forge live scenario using the same harness; verify its trace is exactly Invent -> Make -> Release, Invent folds the roster-bound assignment, Playtest residue is absent except canonical `not-run`, every stage proof validates, and one session identity persists across all three turns.
- [ ] 4.4 Implement the Quest live scenario using the same harness; verify its trace is exactly Invent -> Make -> Playtest -> Release, Playtest is bound to the accepted Made revision, Release is bound to passing evidence, full-tier CAD replay occurs at all applicable gates, and one session identity persists across all four turns.
- [x] 4.5 Add shared topology, passed-through-stage absence, final checkpoint, gate/evidence, publication receipt, credential-isolation, prohibited-activity, context-proof count, and evidence-label assertions for every route; verify targeted mutations name the effort and owning stage rather than producing a generic failure.

## 5. Runner, Execution, and Documentation

- [x] 5.1 Document the route-selectable local command, prerequisites, budgets, diagnostic retention, expected report fields, and the distinction among deterministic CI, real-Codex context acceptance, and full product validation; verify all documented commands support `--help` or preflight without starting a Wish.
- [x] 5.2 Keep credentialed execution outside repository automation and support independent operator-run Spark, Forge, and Quest results through the bounded local command; verify no workflow or required pull-request check is added.
- [x] 5.3 Add `changes/effort-aware-codex-mock-session-e2e.added.md` and update the Concept integration disposition for `1853cfd`, `69afa5c`, `e4eb604`, and `9a979e0`; verify documentation describes published Release, no Match/Concept turns, and the exact current effort routes.
- [x] 5.4 Run the offline mock-session harness/policy suite, required deterministic E2E, relevant runtime/workflow/integration tests, secret scan, and `git diff --check`; record exact commands and ensure ordinary tests require neither Codex authentication nor Factory credentials.
- [ ] 5.5 Run the authenticated Spark/Forge/Quest matrix once on a controlled credentialed environment, verify every sanitized report reaches terminal published Release with the expected turn counts and narrow evidence label, and retain no raw workspace or credential in committed or uploaded artifacts.

## Verification record

- `PYTHONWARNINGS=error /Users/macbookpro/.grid/bin/uv run --frozen python -m unittest tests.runtime.test_codex_native_session tests.workflow.test_native_host -v` — 85 tests passed, including exact-proposal handoff, missing-terminal recovery, same-session bounded retry, unsafe cleanup, identity, and native-turn exhaustion boundaries.
- `PYTHONWARNINGS=error /Users/macbookpro/.grid/bin/uv run --frozen python -m unittest tests.end_to_end.test_mock_session_harness tests.workflow.test_stage_proposal_tool -v` — 41 tests passed; the ordinary harness/finalizer tests used no Codex authentication or Factory credentials.
- `PYTHONWARNINGS=error /Users/macbookpro/.grid/bin/uv run --frozen python -m unittest tests.end_to_end.test_mock_session_harness tests.runtime.test_codex_native_session tests.workflow.test_native_host -v` — 113 tests passed after adding same-checkpoint/different-subject packet coverage and pre-identity fail-closed coverage.
- `PYTHONWARNINGS=error /Users/macbookpro/.grid/bin/uv run --frozen python -m unittest discover -s tests -t . -p 'test_*.py' -v` — 590 tests passed after upstream reconciliation; 18 explicit opt-in scenarios were skipped.
- `WORKSHOP_RUN_DETERMINISTIC_E2E=1 PYTHONWARNINGS=error /Users/macbookpro/.grid/bin/uv run --frozen python -m unittest tests.end_to_end.test_deterministic_native_fidelity -v` — all 17 required deterministic E2E tests passed after the boundary fixture was updated to author the new required chromatic Make render.
- `rg -n --hidden --glob '!mock_session_evidence.py' --glob '!test_mock_session_harness.py' 'mock-session-(factory-password|access-token)-canary|sk-[A-Za-z0-9_-]{20,}' .github changes docs openspec tests tools` — no unexpected fixture canary or API-key-shaped bytes found.
- `git diff --check` — passed.
- `openspec validate add-effort-aware-codex-mock-session-e2e --strict` — passed.

Authenticated Spark reached genuine published `Make -> Release` with two clean context proofs, one start, one resume, one persistent Codex session, exact public manual readback, and zero terminal fallbacks; its sanitized report is `/private/tmp/aw-mock-session-spark-20260828.json`. Authenticated Forge reached genuine published `Invent -> Make -> Release`; the retained trace revalidated with one start, two resumes, one persistent session, three clean context proofs, zero terminal fallbacks, complete/public sealed state, and no fixture-secret canaries.

Quest reached sealed Invent and authored Make on the corrected current topology, but repeated controlled attempts exposed two instruction/fixture mismatches (combined print-target semantics and the routed Playtest `configs/<check-id>.json` contract), which are now explicit and regression-tested. Deeper comparison of the retained Forge and Quest rollouts showed that every actual Codex turn emitted its terminal event and neither session used subagents. After the deterministic Make rejection, the test wrapper's checkpoint-only packet snapshot collided with the new same-checkpoint repair subject and exited before Codex launched; the broad temporary recovery classification then exhausted the 32-turn ceiling. Packet snapshots are now checkpoint-plus-subject bound, and pre-identity incomplete exits fail closed. Redacted diagnostics remain at `/private/var/folders/r2/v780qcr52k508vklthw7xwrm0000gn/T/workshop-mock-session-quest-7x3pih6w`. Tasks 4.4 and 5.5 remain open until Quest reaches published Release on the required clean four-turn route.

A post-fix authenticated Quest run retained at `/private/var/folders/r2/v780qcr52k508vklthw7xwrm0000gn/T/workshop-mock-session-quest-dkd9oxt7` proved the repaired path launches a real same-session turn with a new subject. Its first Make proposal received deterministic `declared-cad-output-changed` feedback; Codex repaired it, then Playtest passed and Release reached terminal published state with exact public readback. All five actual turns observed and forwarded `turn.completed`, used one thread identity, and reported clean context proofs. The acceptance assertion still failed intentionally because the trace was `Invent -> Make -> Make(repair) -> Playtest -> Release`, not the required clean four-turn route. Tasks 4.4 and 5.5 therefore remain open.
